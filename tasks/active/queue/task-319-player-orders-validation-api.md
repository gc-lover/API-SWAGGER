# Task ID: API-TASK-319
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 15:35  
**Создатель:** GPT-5 Codex  
**Зависимости:** [API-TASK-317] – мастер создания заказа (`api/v1/social/player-orders.yaml`), [API-TASK-318] – расчёт бюджета (`api/v1/economy/player-orders/budget.yaml`)

---

## 📋 Краткое описание

Разработать спецификацию `api/v1/world/player-orders/validation.yaml`, описывающую проверки территорий, санкций, юридических ограничений и токсичности контента для заказов игроков.

**Что нужно сделать:** Создать OpenAPI контракт world-service, который выполняет полную валидацию брифа заказа перед публикацией и выдаёт структурированный чеклист для social-service.

---

## 🎯 Цель задания

Мастер создания заказа опирается на world-service для подтверждения легальности и доступности заказов в контексте мира.

**Зачем это нужно:**
- Централизовать правила территории, санкций и правового статуса заказов.
- Предоставить единый API для social-service и economy-service с подробным чеклистом ошибок и предупреждений.
- Обеспечить консистентность UI: пользователи видят те же предупреждения, что фиксируются world-service.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`  
**Версия документа:** v1.0.0  
**Дата последнего обновления:** 2025-11-08 09:53  
**Статус документа:** approved

**Что важно из этого документа:**
- Раздел 8 «Валидация и проверки» — санкции, юридический фильтр, токсичный контент.
- Предусловия (рейтинг, отсутствие санкций, escrow минимумы).
- Интеграции с `world-service`, `factions-service`, `relationships-service`, `content-service`.
- Kafka событие `social.player-orders.validation.failed` и необходимость детализированного отчёта.

### Дополнительные источники

- `.BRAIN/_02-gameplay/social/player-orders-system-детально.md` – глобальный процесс и влияние заказов на мир.  
- `.BRAIN/_02-gameplay/social/player-orders-reputation-детально.md` – влияние санкций и нарушений.  
- `.BRAIN/_02-gameplay/social/relationships-system-детально.md` – whitelists/blacklists.  
- `.BRAIN/_05-technical/content-generation/city-life-population-algorithm.md` – территориальные уровни и риски.  
- `API-SWAGGER/api/v1/world/cities/population.yaml` – пример стиля world-service спецификаций.

### Связанные документы

- `API-SWAGGER/api/v1/social/player-orders.yaml` – orchestration.  
- `API-SWAGGER/api/v1/economy/player-orders/budget.yaml` – взаимозависимость валидаторов бюджета.  
- `API-SWAGGER/api/v1/social/player-orders-components.yaml` – схемы заказов, которые нужно переиспользовать.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/world/player-orders/validation.yaml`  
**Вспомогательные файлы:**  
- `api/v1/world/player-orders/validation-components.yaml` – схемы чеклистов и ограничений.  
- `api/v1/world/player-orders/validation-examples.yaml` – примеры ответов (при необходимости).

**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── world/
            └── player-orders/
                ├── validation.yaml              ← создать
                ├── validation-components.yaml   ← опционально
                └── validation-examples.yaml     ← опционально
```

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)
- **Микросервис:** world-service  
- **Порт:** 8086  
- **API Base Path:** `/api/v1/world/player-orders/*`  
- **Домен:** пространственные ограничения, фракционные санкции, юридические правила  
- **Внешние зависимости:** factions-service (санкции и репутация), security-service (запрещённые действия), economy-service (лимиты бюджета для зон), content-service (токсичность текста), telemetry-service (лог валидаций)

### Frontend (модульная архитектура)
- **Модуль:** `modules/social/player-orders/create-wizard`  
- **State Store:** `useSocialStore` (`validationSummary`, `territoryWarnings`)  
- **UI компоненты @shared/ui:** `OrderValidationSummary`, `ViolationBadge`, `RestrictionDetailsPanel`, `ZoneAccessMap`  
- **Формы @shared/forms:** `ValidationResolutionForm`, `SanctionAppealForm`  
- **Layouts @shared/layouts:** `WizardLayout`, `GameLayout`  
- **Хуки @shared/hooks:** `useWorldMap`, `useFactionPermissions`, `useNotification`, `useRealtime`

### Комментарий для OpenAPI файла
В начале `validation.yaml` зафиксировать микросервис/порт, зависимости (factions, security, content), фронтенд модуль и компоненты, Kafka событие `social.player-orders.validation.failed`, базовый путь `/api/v1/world/player-orders/*`.

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Сбор правил и ограничений
1. Собрать все проверки из `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`.  
2. Классифицировать их по категориям: `territory`, `sanctions`, `legal`, `toxicity`, `budgetBounds`, `duplicates`.  
3. Определить внешние источники данных (фракции, зоны, контент фильтры) и требуемые идентификаторы.

**Результат:** таблица правил с источниками и полями, которые должны вернуться в API.

### Шаг 2: Проектирование endpoints
1. Спроектировать основной endpoint для полного чеклиста (`POST /validation`) и специализированные endpoints для UI (получение правил, статусов зон).  
2. Определить параметры (orderId, zoneId, factionId, template, privacyMode).  
3. Зафиксировать, какие endpoint вызываются синхронно, какие асинхронно (через webhooks/Kafka).

**Результат:** финальный перечень endpoints (см. раздел ниже).

### Шаг 3: Описание моделей данных
1. Создать схемы `ValidationChecklist`, `ValidationIssue`, `TerritoryRestriction`, `SanctionRecord`, `DuplicateOrderHint`.  
2. Добавить severity уровни (`info`, `warning`, `error`), коды правил, рекомендации и пути к remediation.  
3. Вынести повторяющиеся структуры в `validation-components.yaml`.

**Результат:** готовый блок `components` с allOf/oneOf для разных категорий проверок.

### Шаг 4: Безопасность, события и интеграции
1. Подключить `shared/common/security.yaml` (BearerAuth).  
2. Использовать `shared/common/responses.yaml` для типовых ошибок (`422 Validation Failed`, `409 Conflict`, `403 Forbidden`).  
3. Описать `x-kafka-topics` и webhook уведомления для повторного анализа (например, `world.player-orders.validation.audit`).  
4. Указать SLA ответов и требования к idempotency (например, `Validation-Context-Id` header).

**Результат:** спецификация, готовая для интеграции с social-service и telemetry.

### Шаг 5: Примеры и проверка
1. Добавить примеры: успешная валидация, провал из-за санкций, провал из-за зоны, предупреждение по токсичности.  
2. Установить `info.version` на `1.0.0`, указать ссылки на `.BRAIN` документы.  
3. Проверить `npx swagger-cli validate api/v1/world/player-orders/validation.yaml`.  
4. Пройтись по критериям приемки.

**Результат:** валидный YAML с детальной документацией.

---

## 🛣️ Endpoints (предложенная структура)

1. **POST `/world/player-orders/validation`**  
   Принимает полный бриф заказа (`orderId`, `zoneId`, `template`, `objectives`, `riskLevel`, `privacyMode`, `factionId`, `budget`, `documents[]`). Возвращает `ValidationChecklist` со статусами по категориям и сводкой.

2. **GET `/world/player-orders/validation/rules`**  
   Возвращает справочник правил (`ruleCode`, `description`, `severity`, `applicability`) для отображения в UI.

3. **GET `/world/player-orders/zones/{zoneId}/restrictions`**  
   Предоставляет ограничения территории: `legalStatus`, `curfew`, `allowedTemplates`, `requiredPermits`, `sanctionedFactions`, `maxRiskLevel`.

4. **POST `/world/player-orders/validation/sanctions`**  
   Проверяет список участников (заказчик, приглашённые, NPC) на предмет санкций. Возвращает список `SanctionRecord` с рекомендациями.

5. **POST `/world/player-orders/validation/toxicity`**  
   Делегирует проверку текста в `content-service`, но world-service агрегирует результат и возвращает `toxicityScore`, `flaggedPhrases[]`, `moderationRequired`.

6. **POST `/world/player-orders/validation/duplicates`**  
   Отслеживает дублирование заказов (по заголовку, зонам, целям) и возвращает `DuplicateOrderHint[]`.

7. **GET `/world/player-orders/validation/history/{orderId}`** *(опционально)*  
   История прошлых проверок для отображения в арбитраже.

Все endpoints должны поддерживать `locale` параметр (для локализации сообщений) и `traceId` header для telemetry.

---

## 🧱 Основные модели данных

- **ValidationChecklist** – агрегирует категории (`territory`, `sanctions`, `legal`, `toxicity`, `budgetBounds`, `duplicates`) с полями `status`, `issues[]`, `warnings[]`, `summary`.  
- **ValidationIssue** – содержит `ruleCode`, `severity`, `message`, `recommendation`, `blocking`, `serviceSource`, `referenceLinks[]`.  
- **TerritoryRestriction** – `zoneId`, `legalStatus`, `allowedTemplates[]`, `restrictedActions[]`, `curfew`, `securityLevel`, `requiredPermits[]`.  
- **SanctionRecord** – `entityId`, `entityType` (`player`, `faction`, `npc`), `sanctionType`, `severity`, `expiresAt`, `appealUrl`.  
- **DuplicateOrderHint** – `similarOrderId`, `similarityScore`, `reason`, `createdAt`.  
- **ToxicityResult** – `toxicityScore`, `flaggedPhrases[]`, `categories[]`, `moderationRequired`.  
- **ValidationSummary** – `overallStatus`, `blockingIssues`, `warningsCount`, `recommendedActions[]`.

Каждая схема должна иметь `required` поля, enum значений, числовые ограничения и примеры. Общие структуры вынести в `validation-components.yaml`.

---

## 📏 Принципы и правила

- Соблюдать SOLID, DRY, KISS, API First.  
- Не дублировать ответы — использовать `shared/common` компоненты.  
- Максимум 400 строк на файл, схемы и примеры выносить при необходимости.  
- Отражать все зависимости на внешние сервисы и Kafka события.  
- Указать требования к аутентификации (BearerAuth) и к аудит-логам.  
- Валидация должна быть детерминированной и трассируемой — добавить `auditTraceId`.

---

## 🧪 Примеры (минимум добавить в спецификацию)

- Успешная валидация: все категории `passed`, warnings пусты.  
- Провал из-за санкций: `SanctionRecord` для заказчика, severity `error`, рекомендация обратиться в factions-service.  
- Провал из-за зоны: `TerritoryRestriction` запрещает тип `hacker` в зоне `NC-14`, статус `blocked`.  
- Предупреждение токсичности: `toxicityScore=0.74`, список flagged фраз.  
- Предупреждение по дублированию: `similarityScore=0.86`, ссылка на `orderId` активной миссии.

---

## 🔗 Связности и зависимости

- `social-service` – инициирует валидацию перед публикацией и сохраняет `ValidationChecklist` для UI.  
- `economy-service` – использует `TerritoryRestriction` и `budgetBounds` для корректировки коэффициентов.  
- `factions-service` – источник санкций и разрешений.  
- `content-service` – асинхронная проверка токсичности, world-service агрегирует.  
- `relationships-service` – whitelists/blacklists, учитываются при дублировании и санкциях.  
- `telemetry-service` – получает результаты для мониторинга и аналитики.

---

## ✅ Критерии приемки (минимум 11)

1. Файл `api/v1/world/player-orders/validation.yaml` создан с версией `1.0.0` и архитектурным комментарием.  
2. Endpoint `POST /world/player-orders/validation` описывает полный чеклист и возвращает `ValidationChecklist`.  
3. В спецификации есть отдельные endpoints для `rules`, `zones/{zoneId}/restrictions`, `sanctions`, `toxicity`, `duplicates`.  
4. Все ответы используют общие компоненты ошибок и описывают статусы `passed`, `warning`, `failed`.  
5. Схемы включают `ruleCode`, `severity`, `blocking`, `recommendation`, `serviceSource`.  
6. Описаны Kafka события и/или webhooks: `social.player-orders.validation.failed`, `world.player-orders.validation.audit`.  
7. Документация учитывает зависимость от `factions-service` и `content-service`.  
8. Добавлены примеры для успешных и провальных сценариев, соответствующих схемам.  
9. Команда `npx swagger-cli validate api/v1/world/player-orders/validation.yaml` проходит без ошибок.  
10. Объем файлов ≤ 400 строк, общие схемы вынесены при необходимости.  
11. Дополнительно описан `ValidationSummary` для отображения в UI.  
12. Указаны требования к audit-trail (`auditTraceId`, `Validation-Context-Id`).

---

## ❓ FAQ

**Q:** Почему world-service отвечает за токсичность, если этим занимается content-service?  
**A:** Проверка токсичности инициируется world-service, чтобы собрать полный чеклист; сам анализ выполняет content-service, но world-service агрегирует результат и даёт единый ответ.

**Q:** Как обрабатываются локализованные сообщения об ошибках?  
**A:** Вместе с `ruleCode` возвращается `messageKey`, а клиент подтягивает локализацию. Для быстрых решений можно включить `localizedMessage` по `locale` query.

**Q:** Что делать, если проверка зоны требует продолжительного анализа?  
**A:** Endpoint может вернуть статус `pending` с `retryAfter`. В этом случае world-service публикует `world.player-orders.validation.audit` при завершении, и social-service повторяет запрос.

**Q:** Как интегрируется проверка дубликатов с relationships-service?  
**A:** В `DuplicateOrderHint` передаётся признак `trustedRelationship`, если заказ совпадает с закрытым списком. Social-service использует это для дополнительной модерации.

**Q:** Какие данные логируются для аудита?  
**A:** `auditTraceId`, `ruleCodes`, входные параметры проверки и ссылки на источники данных. Это нужно для арбитража и compliance.

---

## 📌 История выполнения

- 2025-11-08 – задание создано на основе `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`, статус `queued`.

# Task ID: API-TASK-319
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 12:40  
**Создатель:** GPT-5 Codex  
**Зависимости:** [API-TASK-317] — мастер создания заказов (social-service)

---

## 📋 Краткое описание

Разработать OpenAPI спецификацию `api/v1/world/player-orders/validation.yaml`, обеспечивающую многоуровневую проверку заказов игроков: юридические ограничения, санкции, токсичный контент, соответствие зонам и лимитам бюджета.

**Что нужно сделать:** Создать новый контракт world-service для комплексной валидации брифа заказа на основе `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md` и связанных документов.

---

## 🎯 Цель задания

Предоставить social-service и фронтенду централизованный сервис проверок, который подтверждает готовность заказа к публикации и синхронизирует статус с economy и factions сервисами.

**Зачем это нужно:**
- Гарантировать соблюдение мировых и юридических правил перед публикацией заказа.
- Обеспечить единый чеклист валидации для UI и автоматического мониторинга.
- Повысить доверие к системе заказов за счёт детализированных отчетов и рекомендаций.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`  
**Версия документа:** v1.0.0  
**Дата последнего обновления:** 2025-11-08 09:53  
**Статус документа:** approved  

**Что важно из этого документа:**
- Обязательные проверки: полнота полей, юридический фильтр, санкции, токсичный контент, минимальные/максимальные бюджеты.
- Интеграции с world-service (территории), factions-service (репутация и санкции), content-service (токсичность), economy-service (лимиты бюджета).
- Логирование ошибок в telemetry-service и повторные проверки после исправлений.

### Дополнительные источники

- `.BRAIN/_02-gameplay/social/player-orders-system.md` — жизненный цикл заказов.
- `.BRAIN/_02-gameplay/social/player-orders-reputation-детально.md` — влияние санкций и нарушений.
- `.BRAIN/_02-gameplay/social/player-orders-advanced.md` — расширенные правила и edge cases.
- `.BRAIN/_02-gameplay/world/world-state/player-impact-systems.md` — зоны влияния и ограничения.
- `.BRAIN/_02-gameplay/social/relationships-system-детально.md` — адресные приглашения и доверие.
- `.BRAIN/_02-gameplay/social/npc-hiring-system-детально.md` — правила для NPC исполнителей.

### Связанные API

- `API-SWAGGER/api/v1/social/player-orders.yaml` — основной мастер (API-TASK-317).
- `API-SWAGGER/api/v1/world/cities/population.yaml` — контексты территорий и рисков.
- `API-SWAGGER/api/v1/shared/common/responses.yaml`, `security.yaml` — стандартные компоненты.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/world/player-orders/validation.yaml`  
**Вспомогательные файлы:**  
- `api/v1/world/player-orders/validation-components.yaml` (если потребуется вынести схемы)  
- Общие компоненты: `api/v1/shared/common/responses.yaml`, `security.yaml`

**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── world/
            └── player-orders/
                ├── validation.yaml              ← создать
                └── validation-components.yaml   ← создать при необходимости
```

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)
- **Микросервис:** world-service  
- **Порт:** 8086  
- **API Base Path:** `/api/v1/world/player-orders/*`  
- **Интеграции:** social-service (бриф заказа), economy-service (пороговые значения бюджета), factions-service (санкции и репутация), content-service (токсичный контент), telemetry-service (логирование), auth-service (аутентификация).
- **Kafka события:** `world.player-orders.validation.started`, `world.player-orders.validation.completed`, `world.player-orders.validation.failed`.

### Frontend (модульная архитектура)
- **Модуль:** `modules/social/player-orders/create-wizard`  
- **State Store:** `useSocialStore` (`validationChecklist`, `issueFeed`)  
- **UI компоненты @shared/ui:** `OrderValidationSummary`, `IssueList`, `RiskLevelBadge`, `SanctionAlert`, `ChecklistProgress`  
- **Формы @shared/forms:** `OrderValidationReviewForm`, `InviteListForm` (для отображения санкций)  
- **Layouts @shared/layouts:** `WizardLayout`, `GameLayout`  
- **Хуки @shared/hooks:** `useValidationPolling`, `useRealtime`, `useFactionPermissions`, `useContentModeration`

### Комментарий для OpenAPI файла
В верхней части `validation.yaml` зафиксировать микросервис, модуль фронтенда, используемые компоненты, хуки и Kafka события.

---

## ✅ Детальный план работ

1. **Собрать требования:** выписать сценарии проверок из `.BRAIN` документов, разделить на синхронные и асинхронные.
2. **Определить эндпоинты:** запуск валидации, получение результатов, проверка отдельных чеков, предоставление рекомендаций.
3. **Спроектировать модели:** `ValidationRequest`, `ValidationChecklist`, `ValidationIssue`, `SanctionRecord`, `ToxicityReport`, `Recommendation`.
4. **Настроить статусы и события:** `pending`, `in_progress`, `passed`, `failed`, `needs_attention`.
5. **Описать интеграции:** как сервис обращается к economy, factions, content, npc, relationships.
6. **Указать бизнес-правила:** обязательные поля, лимиты бюджета, уровни санкций, токсичности.
7. **Добавить примеры:** успешная валидация, провал по санкциям, предупреждение о токсичном контенте, превышение бюджета.
8. **Проверить OpenAPI:** вынести повторяющиеся структуры в `validation-components.yaml`, запустить `npx swagger-cli validate`.

---

## 🛣️ Предложенные endpoints

1. **POST `/world/player-orders/{orderId}/validation`** — запускает полный чеклист, возвращает `validationId`, статус `pending`.
2. **GET `/world/player-orders/{orderId}/validation/{validationId}`** — текущий статус, результаты чеков, рекомендации.
3. **POST `/world/player-orders/{orderId}/validation/checks/{check}`** — ручной запуск/повторный запуск конкретного чекпойнта (`completeness`, `legal`, `sanctions`, `toxicity`, `budget`).
4. **GET `/world/player-orders/{orderId}/validation/issues`** — список актуальных `ValidationIssue[]` с пагинацией и фильтрами по `severity`.
5. **GET `/world/player-orders/{orderId}/validation/recommendations`** — контекстные рекомендации по устранению проблем.
6. **GET `/world/player-orders/{orderId}/sanctions`** — детали санкций и ограничений из factions-service.
7. **GET `/world/player-orders/{orderId}/toxicity`** — отчёт по токсичности от content-service.
8. **POST `/world/player-orders/{orderId}/validation/acknowledge`** — подтверждение знакомство с предупреждениями (для публикации при отклонениях медианы/рисков).

Все методы используют общие ответы `400/401/403/404/409/422/500` через `$ref` и параметр `orderId` из компонентов.

---

## 🧱 Основные модели

- **ValidationRequest** — `orderId`, `template`, `riskLevel`, `budgetEstimate`, `visibility`, `invitedMembers[]`, `npcAgents[]`, `isCorporate`.
- **ValidationChecklist** — список шагов (`completeness`, `legal`, `sanctions`, `toxicity`, `budget`, `relationships`) с `status`, `severity`, `checkedAt`, `serviceSource`.
- **ValidationIssue** — `issueId`, `category`, `severity` (info/warning/error/blocker), `description`, `recommendation`, `sourceService`, `detectedAt`, `remediationDeadline?`.
- **SanctionRecord** — `factionId`, `sanctionType`, `expiresAt`, `appealAvailable`, `penalties`.
- **ToxicityReport** — `score`, `threshold`, `phrases[]`, `suggestedReplacements[]`.
- **BudgetConstraint** — `minBudget`, `maxBudget`, `median`, `deviation`, `requiresApproval`.
- **ValidationResultSummary** — `status`, `issuesCount`, `warningsCount`, `passedChecks`, `failedChecks`, `nextActions`.
- **ValidationEvent** — `eventId`, `eventType`, `timestamp`, `payload`.

---

## 📈 Бизнес-правила и ограничения

- Все обязательные поля брифа должны быть заполнены; отсутствие любого критического поля → `severity: blocker`.
- Заказы в запрещённых зонах (`world-service` blacklist) отклоняются автоматически.
- Если бюджет < минимального порога или > максимального — `severity: warning` с требованием подтверждения; повторная отправка без изменений → `409`.
- При активных санкциях у заказчика или приглашённых — блокировать публикацию, вернуть список санкций.
- Токсичный контент (`toxicityScore > threshold`) требует устранения; при серьёзных нарушениях → `severity: blocker`.
- Корпоративные заказы должны иметь `factionAuthorizationId`.
- Валидация должна фиксировать `auditTrail` с указанием источника каждого нарушения.

---

## 📊 Критерии приемки (минимум 10)

1. Файл `api/v1/world/player-orders/validation.yaml` создан и проходит `npx swagger-cli validate`.
2. В начале файла есть архитектурный комментарий из секции «Целевая архитектура».
3. Реализован запуск полной валидации и повторных чеков.
4. Описаны модели `ValidationChecklist`, `ValidationIssue`, `SanctionRecord`, `ToxicityReport`.
5. Для каждого чекпойнта определены статусы и уровни серьёзности.
6. Документированы интеграции с economy, factions, content, relationships, npc сервисами.
7. Приведены примеры ответов для успешного и проваленного чеклистов.
8. Все ошибки используют общие ответы из `shared/common/responses.yaml`.
9. `security` подключает схемы из `shared/common/security.yaml`.
10. Добавлены Kafka события с payload примерами.
11. Спецификация ≤400 строк либо вынесены компоненты в отдельный файл.
12. Прописан чеклист в конце задания и связь с обновлением `.BRAIN` документа.

---

## ❓ FAQ

- **В:** Как часто можно запускать повторную валидацию?  
  **О:** Не ограничивать, но логировать каждую попытку; при >5 попыток за час возвращать предупреждение.

- **В:** Что делать, если сервис зависимостей недоступен?  
  **О:** Возвращать `503` с описанием недоступного сервиса и рекомендовать повтор через `retryAfter`.

- **В:** Можно ли опубликовать заказ при предупреждениях?  
  **О:** Да, если все блокирующие issues устранены и заказчик подтвердил предупреждения через `/validation/acknowledge`.

- **В:** Как обрабатывать NPC приглашения?  
  **О:** Проверять наличие контракта и доступности NPC через npc-service; иначе создавать `warning` с рекомендацией замены.

---

## 🧾 Отчётность и интеграции

- Событие `world.player-orders.validation.started` публикуется при запуске чеклиста (payload: `orderId`, `validationId`, `startedAt`).
- Событие `world.player-orders.validation.failed` содержит список блокирующих issues.
- Логи направляются в telemetry-service (`validationId`, `issuesCount`, `durationMs`).
- Необходимо синхронизировать статусы с social-service (через REST или события) для отображения прогресса в мастере.

---

**Чеклист перед сдачей:**  
- [ ] Задокументированы все чекпойнты и модели.  
- [ ] Добавлены примеры успешного/неуспешного выполнения.  
- [ ] Описаны Kafka события.  
- [ ] Файл прошёл `npx swagger-cli validate`.  
- [ ] Обновлены `brain-mapping.yaml` и `.BRAIN` документ.


