# Task ID: API-TASK-318
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** completed  
**Создано:** 2025-11-08 16:10  
**Создатель:** GPT-5 Codex  
**Зависимости:** API-TASK-317 (social-service мастер заказов), API-TASK-319 (world-service валидатор)

---

## 📋 Краткое описание

Спроектировать OpenAPI спецификацию `api/v1/economy/player-orders/budget.yaml`, описывающую расчёт бюджета, escrow и страховых гарантий заказов игроков.

**Что нужно сделать:** Создать новый файл спецификации economy-service с REST endpoints, моделями данных, Kafka событиями и примерами, основанный на требованиях `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`.

---

## 🎯 Цель задания

Обеспечить social-service и фронтенд мастера заказов детерминированным API для симуляции бюджета, проверки корректности значений и управления финансовыми гарантиями.

**Зачем это нужно:**
- Социалка должна получать расчёт `BaseReward`, `Escrow`, `Commission` и страховые тарифы из одного сервиса.
- Экономика контролирует соблюдение формул и порогов, снижая риск мошенничества.
- Фронтенд отображает breakdown бюджета и предупреждения игроку на каждом шаге мастера.

---

## 📚 Источники информации

### Основной источник
**Репозиторий:** `.BRAIN`  
**Документ:** `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`  
**Версия:** v1.0.0 (approved)  
**Последнее обновление:** 2025-11-08 09:53  

**Важные аспекты:**
- Формула `BaseReward = ComplexityScore * RiskModifier * MarketIndex * TimeModifier`.
- Диапазоны коэффициентов: `riskModifier 0.8–1.5`, `timeModifier 1.0–1.3`, `escrowRate 10–30%`, `commissionRate 5–12%`.
- Требование сравнивать результат с медианой шаблона и предупреждать о отклонениях.
- Необходимость страховых пакетов (basic/extended/premium) и уведомлений о завышении/занижении бюджета.

### Дополнительные источники
- `.BRAIN/_02-gameplay/social/player-orders-system-детально.md` — жизненный цикл заказов.
- `.BRAIN/_02-gameplay/social/player-orders-economy.md` — рыночные индексы, escrow и комиссии.
- `.BRAIN/_02-gameplay/social/player-orders-reputation-детально.md` — влияние бюджета на рейтинги.
- `.BRAIN/_02-gameplay/social/relationships-system-детально.md` — коэффициенты для адресных приглашений.
- JSON схемы: `schemas/social/player-order-budget.schema.json`, `schemas/social/player-order-publish.schema.json`.
- Спецификация `api/v1/social/player-orders.yaml` — orchestration social-service.

### Связанные задачи/API
- API-TASK-317 (`api/v1/social/player-orders.yaml`) — мастер заказа.
- Планируемая спецификация `api/v1/world/player-orders/validation.yaml` (API-TASK-319) — валидатор зон и санкций.

---

## 📁 Целевая структура API

- **Репозиторий:** `API-SWAGGER`  
- **Целевой файл:** `api/v1/economy/player-orders/budget.yaml`  
- **Версия API:** v1  
- **Тип:** OpenAPI 3.0.3 YAML  

```
api/
└── v1/
    └── economy/
        └── player-orders/
            ├── budget.yaml                ← создать
            ├── budget-components.yaml     ← создать при необходимости (разбиение ≤400 строк)
            └── README.md                  ← обновить кратким описанием службы (опционально)
```

Использовать общие компоненты: `api/v1/shared/common/responses.yaml`, `api/v1/shared/common/pagination.yaml`, `api/v1/shared/common/security.yaml`.

---

## 🏗️ Целевая архитектура

### Backend
- **Микросервис:** economy-service  
- **Порт:** 8085  
- **Базовый путь:** `/api/v1/economy/player-orders/*`  
- **Интеграции:** world-service (риск зон), social-service (брифы), factions-service (корпоративные коэффициенты), telemetry-service ( аудит расчётов), notification-service (уведомления escrow).  
- **Kafka:** `economy.player-orders.budget.estimated`, `economy.player-orders.escrow.locked`, `economy.player-orders.insurance.applied`.

### Frontend
- **Модуль:** `modules/social/player-orders/budget`  
- **State Store:** `useSocialStore` (`budgetSimulator`, `escrowState`)  
- **UI:** `BudgetSimulator`, `EscrowSummary`, `RiskIndicator`, `InsuranceTierCard`  
- **Forms:** `BudgetAdjustForm`, `GuaranteeSelectionForm`  
- **Hooks:** `useEconomyRates`, `useMarketIndex`, `useRealtime`, `useDebounce`

**Комментарий:** добавить блок `Target Architecture` в начале `budget.yaml`, фиксируя микросервис, фронтенд модуль, UI/формы, Kafka события, зависимости.

---

## ✅ Детальный план выполнения

1. **Анализ источников** — извлечь коэффициенты, ограничения и сценарии из `.BRAIN` документов; сверить JSON схемы бюджета.  
2. **Проектирование endpoints** — определить синхронные и асинхронные расчёты, справочники индексов, операции escrow и страховых планов.  
3. **Модели данных** — описать `BudgetEstimateRequest`, `BudgetEstimateResponse`, `EscrowQuote`, `InsuranceTier`, `MarketIndexSnapshot`, `BudgetWarning`.  
4. **Бизнес-правила** — задокументировать диапазоны, обработку медианы, профили заказов (combat, hacker, economic и т.д.).  
5. **Коды ошибок и безопасность** — использовать общие ответы (`400/401/403/404/409/422/500`), включить BearerAuth с ролями `economy.operator`, `social-service`.  
6. **Kafka и асинхронность** — описать события и поля payload; указать idempotency (`X-Budget-Calculation-Id`).  
7. **Примеры** — подготовить минимум три примера: стандартный заказ, корпоративный заказ, сценарий с предупреждениями.  
8. **Валидация** — проверить файл командой `npx swagger-cli validate api/v1/economy/player-orders/budget.yaml`; пройти чеклист `tasks/config/checklist.md`.

---

## 🧩 Endpoints (минимальный набор)

1. **POST `/api/v1/economy/player-orders/budget/estimate`** — синхронный расчёт бюджета и escrow.  
2. **POST `/api/v1/economy/player-orders/budget/estimate/jobs`** — запуск асинхронного расчёта; ответ содержит `jobId`.  
3. **GET `/api/v1/economy/player-orders/budget/estimate/jobs/{jobId}`** — статус асинхронного расчёта.  
4. **GET `/api/v1/economy/player-orders/market-index`** — текущие и исторические значения `MarketIndex` с фильтрами `district`, `faction`, `interval`.  
5. **GET `/api/v1/economy/player-orders/insurance-tiers`** — справочник страховых пакетов и коэффициентов.  
6. **POST `/api/v1/economy/player-orders/{orderId}/escrow/lock`** — предварительная блокировка escrow.  
7. **DELETE `/api/v1/economy/player-orders/{orderId}/escrow/lock`** — отмена блокировки до публикации.  
8. **POST `/api/v1/economy/player-orders/{orderId}/budget/compare`** — сравнение предложенного бюджета с медианой/рекомендациями.

Дополнительно предусмотреть параметры для валют, шаблонов, риска (`riskLevel`), а также заголовки трассировки (`X-Trace-Id`).

---

## 🧱 Ключевые модели

- `PlayerOrderBudgetEstimateRequest` — включает `complexityScore`, `riskModifier`, `marketIndex`, `timeModifier`, `templateCode`, `districtCode`, `isCorporate`, `preferredCurrency`, `guaranteeTier`.  
- `PlayerOrderBudgetEstimateResponse` — поля `baseReward`, `escrow`, `commission`, `insuranceFee`, `recommendedBudgetRange`, `warnings[]`, `calculationBreakdown`.  
- `BudgetWarning` — `code`, `severity`, `messageKey`, `suggestedAction`, `threshold`.  
- `MarketIndexSnapshot` — `value`, `volatility`, `source`, `timestamp`, `nextRefreshAt`.  
- `InsuranceTier` — `id`, `name`, `coverage`, `commissionRate`, `escrowMultiplier`, `bonusReputation`.  
- `EscrowLockRequest` / `EscrowLockResponse` — `orderId`, `ownerId`, `escrowAmount`, `lockExpiresAt`, `status`, `insuranceTier`.  
- `BudgetComparisonResult` — `proposed`, `recommended`, `median`, `deviationPercent`, `warnings[]`.

Вынести повторяющиеся структуры в `budget-components.yaml`, добавить `required`, `description`, `minimum/maximum`, `enum`, `example`.

---

## 📌 Принципы и правила

- Соблюдать SOLID, DRY, KISS, API First.  
- Не дублировать стандартные ответы — ссылаться на `shared/common`.  
- Файл ≤ 400 строк; при превышении вынести схемы/примеры.  
- Использовать семантическое версионирование (`info.version: 1.0.0`).  
- Не хардкодить коэффициенты — описывать диапазоны и источники.  
- Фиксировать Kafka события в `x-kafka-topics` с payload схемами.  
- Указать требования к аудит-логу (`x-audit` или описание в тексте).

---

## ✅ Критерии приёмки

1. Создан файл `api/v1/economy/player-orders/budget.yaml` с блоком `Target Architecture`.  
2. Описаны endpoints для синхронного и асинхронного расчёта, escrow, страховых пакетов, сравнения бюджета.  
3. Модели данных содержат все поля и ограничения, перечисленные в документе `.BRAIN`.  
4. Реализован массив предупреждений с кодами (`BUDGET_OVERESTIMATED`, `MARKET_SPIKE`, `RISK_HIGH`).  
5. Подключены стандартные ответы (`401/403/404/409/422/500`) через `$ref`.  
6. Добавлены Kafka события в `x-kafka-topics` с описанием payload.  
7. Примеры покрывают минимум три сценария (норма, корпоративный заказ, предупреждения).  
8. Указаны требования к аутентификации и ролям.  
9. Прохождение `npx swagger-cli validate` задокументировано в истории.  
10. Чеклист `tasks/config/checklist.md` отмечен как выполненный.  
11. При необходимости создан `budget-components.yaml`, обеспечивающий ≤400 строк.

---

## ❓ FAQ

- **В:** Обязателен ли асинхронный расчёт?  
  **О:** Для заказов с `complexityScore` выше порога или множеством этапов — да; спецификация должна описать `jobs` и статусы.  
- **В:** Как передавать пользовательские корректировки?  
  **О:** Через `manualAdjustment` внутри запроса; ответ должен включать `auditTrail`.  
- **В:** Что делать при всплеске `MarketIndex`?  
  **О:** Возвращать предупреждение `MARKET_SPIKE` и рекомендовать повторить расчёт после `nextRefreshAt`.  
- **В:** Какие роли вызывают `escrow/lock`?  
  **О:** `social-service` и `arbitration-service` с scope `economy.player-orders.write`.  
- **В:** Нужна ли поддержка разных валют?  
  **О:** Да, предусмотреть `preferredCurrency` и описание конвертации в `calculationBreakdown`.

---

## 🧭 История выполнения

- 2025-11-08 16:10 — задача создана, статус `queued`.
- 2025-11-08 16:18 — взято в работу, подготовлены статусы в .BRAIN и brain-mapping.
- 2025-11-08 16:24 — создана спецификация `api/v1/economy/player-orders/budget.yaml` + `player-orders-budget-components.yaml`.
- 2025-11-08 16:26 — `npx swagger-cli validate api/v1/economy/player-orders/budget.yaml` — **OK**.
- 2025-11-08 16:45 — работа начата, статус `in_progress`.

---

**Следующие шаги:** После подготовки спецификации — обновить `brain-mapping.yaml`, зафиксировать прогресс в `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`, запустить `npx swagger-cli validate` и приложить результат в Истории задачи.
# Task ID: API-TASK-318
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** in_progress  
**Создано:** 2025-11-08 12:35  
**Создатель:** GPT-5 Codex  
**Зависимости:** [API-TASK-317] — создание мастера заказов (social-service)

---

## 📋 Краткое описание

Спроектировать и описать OpenAPI спецификацию `api/v1/economy/player-orders/budget.yaml`, обеспечивающую расчёт бюджета, escrow и комиссий для заказов игроков с учётом формул сложности, рыночных индексов и гарантий.

**Что нужно сделать:** Создать новую спецификацию economy-service для расчётов бюджета заказов игроков на основе `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md` и сопутствующих документов по экономике.

---

## 🎯 Цель задания

Дать economy-service контракт для синхронных и асинхронных расчётов бюджета, escrow и страховок, используемых мастером создания заказов.

**Зачем это нужно:**
- Обеспечить точный расчёт `BaseReward`, `Escrow`, `Commission` и страховых тарифов по формуле сложностей.
- Согласовать REST API между social-service и economy-service для симуляции бюджета и блокировки средств.
- Предоставить фронтенду стабильный интерфейс для визуализации бюджета и подсказок игроку.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`  
**Версия документа:** v1.0.0  
**Дата последнего обновления:** 2025-11-08 09:53  
**Статус документа:** approved  

**Что важно из этого документа:**
- Формула бюджета `BaseReward = ComplexityScore * RiskModifier * MarketIndex * TimeModifier`.
- Диапазоны `RiskModifier`, `TimeModifier`, ставки escrow (10–30%), комиссии (5–12%).
- Требование сравнивать результат с медианой по шаблону заказа и предупреждать при отклонениях.
- Потребность в симуляции бюджета перед публикацией и логировании расчётов.

### Дополнительные источники

- `.BRAIN/_02-gameplay/social/player-orders-economy.md` — escrow, страхование, комиссии.
- `.BRAIN/_02-gameplay/social/player-orders-reputation-детально.md` — влияние бюджета на репутацию.
- `.BRAIN/_02-gameplay/social/player-orders-system.md` — общий жизненный цикл заказов.
- `.BRAIN/_02-gameplay/social/relationships-system-детально.md` — тарифы для адресных приглашений.
- `.BRAIN/05-technical/economy/escrow-pipeline.md` — политика escrow и страховых планов.
- Существующие спецификации: `api/v1/social/player-orders.yaml`, `api/v1/social/player-orders-components.yaml`.

### Связанные документы и API

- `API-SWAGGER/api/v1/social/player-orders.yaml` — мастер создания заказа (обновляется по задаче API-TASK-317).
- `API-SWAGGER/api/v1/economy/shared/rates.yaml` (если существует) — референсы на рыночные индексы.
- Kafka события `economy.player-orders.escrow.locked`, `economy.player-orders.budget.updated`.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/economy/player-orders/budget.yaml`  
**Вспомогательные файлы:**  
- `api/v1/economy/player-orders/budget-components.yaml` (если объём >400 строк)  
- Общие компоненты: `api/v1/shared/common/responses.yaml`, `api/v1/shared/common/pagination.yaml`, `api/v1/shared/common/security.yaml`

**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── economy/
            └── player-orders/
                ├── budget.yaml                 ← создать
                └── budget-components.yaml      ← создать при необходимости
```

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)
- **Микросервис:** economy-service  
- **Порт:** 8085  
- **API Base Path:** `/api/v1/economy/player-orders/*`  
- **Интеграции:** social-service (мастер заказа), world-service (данные по зонам), factions-service (корпоративные коэффициенты), telemetry-service (логирование расчётов), auth-service (аутентификация).
- **Kafka события:** `economy.player-orders.budget.estimated`, `economy.player-orders.escrow.locked`, `economy.player-orders.escrow.released`.

### Frontend (модульная архитектура)
- **Модуль:** `modules/social/player-orders/create-wizard`  
- **State Store:** `useSocialStore` (`budgetSimulator`, `escrowStatus`)  
- **UI компоненты @shared/ui:** `BudgetSimulator`, `RewardBreakdownCard`, `EscrowStatusBanner`, `RiskIndicator`  
- **Формы @shared/forms:** `OrderBudgetForm`, `EscrowPlanSelector`, `InsuranceTierSelector`  
- **Layouts @shared/layouts:** `WizardLayout`, `GameLayout`  
- **Хуки @shared/hooks:** `useEconomyRates`, `useMarketIndex`, `useRealtime`, `useDebounce`

### Комментарий для OpenAPI файла
В начале `budget.yaml` добавить архитектурный блок с выбранным микросервисом, фронтенд модулем, UI/формами, Kafka событиями и базовым путём API.

---

## ✅ Детальный план работ

1. **Анализ источников:** вытащить коэффициенты, ограничения и кейсы из `.BRAIN` документов.
2. **Определить эндпоинты:** расчёт бюджета, получение текущего рыночного индекса, блокировка escrow, управление страховыми планами.
3. **Спроектировать модели данных:** `BudgetEstimateRequest`, `BudgetEstimateResponse`, `EscrowQuote`, `CommissionBreakdown`, `MarketIndexSnapshot`, `InsurancePlan`.
4. **Определить бизнес-правила:** проверки диапазонов коэффициентов, медиана по шаблону, округление сумм, лимиты escrow.
5. **Настроить события и асинхронные процессы:** job-сущности для длительных расчётов, статусы `queued/running/completed/failed`.
6. **Описать ошибки и безопасность:** использовать общие ответы, описать `409` для конфликтов медианы, `422` для неверных коэффициентов.
7. **Добавить примеры:** успешный расчёт боевого заказа, предупреждение о завышенном бюджете, корпоративный заказ с премиум-гарантией.
8. **Проверка и валидация:** убедиться, что спецификация <400 строк (вынести компоненты), запустить `npx swagger-cli validate`.

---

## 🛣️ Предложенные endpoints

1. **POST `/economy/player-orders/estimate`** — синхронный расчёт бюджета по входным коэффициентам, возвращает `BudgetEstimateResponse` с рекомендациями.
2. **POST `/economy/player-orders/estimate/jobs`** — запуск асинхронного расчёта для крупных заказов, выдаёт `jobId`.
3. **GET `/economy/player-orders/estimate/jobs/{jobId}`** — статус расчёта (`queued/running/completed/failed`), прогресс, промежуточные метрики.
4. **GET `/economy/player-orders/market-index`** — текущий `MarketIndex` и прогноз на 24 часа.
5. **POST `/economy/player-orders/{orderId}/escrow`** — предварительная блокировка escrow, проверка доступности средств, возврат `EscrowQuote`.
6. **DELETE `/economy/player-orders/{orderId}/escrow`** — отмена блокировки escrow (до публикации заказа).
7. **GET `/economy/player-orders/{orderId}/commission`** — детализация комиссий и страховых премий.
8. **GET `/economy/player-orders/templates/{template}/median`** — медианные значения по шаблону заказа, пределы и рекомендации.
9. **POST `/economy/player-orders/{orderId}/insurance`** — выбор страхового плана, возврат подтверждения и тарифов.

Все эндпоинты должны ссылаться на общие ответы (`400/401/403/404/409/422/500`) и использовать компонент `orderId` в `parameters`.

---

## 🧱 Ключевые модели

- **BudgetEstimateRequest** — включает `complexityFactors`, `riskModifier`, `marketIndexOverride?`, `timeModifier`, `bonuses[]`, `penalties[]`, `template`, `playerRank`.
- **BudgetEstimateResponse** — `baseReward`, `escrow`, `commission`, `insuranceOptions[]`, `median`, `medianDeviation`, `warnings[]`, `timestamp`.
- **ComplexityFactors** — детализация факторов (`zones[]`, `skillRequirements[]`, `missionStages`, `hostileFactions[]`).
- **EscrowQuote** — `orderId`, `escrowAmount`, `lockUntil`, `insuranceTier`, `status`.
- **CommissionBreakdown** — `commissionRate`, `commissionAmount`, `platformFee`, `insuranceFee`, `factionFee`.
- **InsurancePlan** — `tier` (basic/extended/premium), `coverage`, `cost`, `payoutRules`, `bonusRating`.
- **BudgetEstimateJob** — `jobId`, `status`, `submittedAt`, `startedAt`, `completedAt`, `progress`, `result`, `errors[]`.
- **MarketIndexSnapshot** — `value`, `trend`, `source`, `lastUpdated`, `nextRefresh`.

Все модели обязаны иметь `required`, `description`, `minimum/maximum`, `enum`, `example`. Повторяемые структуры вынести в `budget-components.yaml`.

---

## 📈 Бизнес-правила и ограничения

- Диапазоны коэффициентов: `riskModifier` 0.8–1.5, `timeModifier` 1.0–1.3, `escrowRate` 0.1–0.3, `commissionRate` 0.05–0.12.
- Минимальный `baseReward` ≥ 100 (конфигурируемо), иначе возвращать предупреждение.
- При отклонении бюджета от медианы > ±25% возвращать `warning` и `409` при попытке блокировки escrow без подтверждения.
- В корпоративном режиме (`isCorporate = true`) обязательна проверка `factionAuthorizationId`.
- Для заказов с высоким риском (`riskLevel ≥ severe`) escrow минимум 20%.
- Любые override значений требуют флага `manualOverride` и причин в `auditTrail`.

---

## 📊 Критерии приемки (минимум 10)

1. Файл `api/v1/economy/player-orders/budget.yaml` создан и валиден по OpenAPI 3.0.3.
2. В начале файла присутствует архитектурный комментарий из секции «Целевая архитектура».
3. Предусмотрены синхронные и асинхронные расчёты бюджета (`/estimate`, `/estimate/jobs`).
4. Прописаны модели `BudgetEstimateRequest` и `BudgetEstimateResponse` с примерами.
5. В спецификации задокументированы диапазоны коэффициентов и правила медианы.
6. Описаны эндпоинты управления escrow и страховыми планами.
7. Все ответы ошибок используют `api/v1/shared/common/responses.yaml`.
8. Списки и логи расчётов используют пагинацию из `shared/common/pagination.yaml` (если применимо).
9. Добавлены Kafka события в `x-kafka-topics` и описаны события `economy.player-orders.*`.
10. Примеры покрывают минимум три сценария: боевой заказ, корпоративный заказ, заказ с завышенным бюджетом.
11. В `security` используется схема из `api/v1/shared/common/security.yaml`.
12. Запуск `npx swagger-cli validate api/v1/economy/player-orders/budget.yaml` проходит без ошибок.

---

## ❓ FAQ

- **В:** Что делать, если `MarketIndex` устарел?  
  **О:** Вернуть предупреждение и поле `nextRefresh`, блокировать escrow только после обновления или ручного подтверждения.

- **В:** Как обрабатывать ручные корректировки бюджета?  
  **О:** Требовать `manualOverride` и массив `adjustments[]` с описанием причины; логировать в telemetry-service.

- **В:** Можно ли пропустить асинхронный расчёт?  
  **О:** Да, для простых заказов использовать синхронный `/estimate`. Асинхронный режим обязателен при `complexityScore > threshold`.

- **В:** Как учитывать скидки гильдий?  
  **О:** Добавить `guildDiscount` в `CommissionBreakdown`, данные берутся из economy-service; при отсутствии гильдии поле равно 0.

---

## 🧾 Отчётность и интеграции

- Логировать все расчёты в telemetry-service с полями `orderId`, `baseReward`, `escrow`, `deviation`.
- Публиковать события `economy.player-orders.budget.estimated` после успешного расчёта (payload включает `orderId`, `baseReward`, `deviation`, `warnings`).
- При блокировке escrow публиковать `economy.player-orders.escrow.locked` с суммой и страховым планом.
- Обновить документацию `player-orders-creation` после завершения задачи (см. brain-mapping).

---

**Чеклист перед сдачей:**  
- [ ] Все эндпоинты и модели описаны.  
- [ ] Примеры покрывают ключевые сценарии.  
- [ ] Файл ≤400 строк (иначе компоненты вынесены).  
- [ ] Пройдена валидация `npx swagger-cli validate`.  
- [ ] Обновлены `brain-mapping.yaml` и `.BRAIN` документ.

