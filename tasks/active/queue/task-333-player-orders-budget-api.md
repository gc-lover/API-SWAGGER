# Task ID: API-TASK-ECON-PO-Budget
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 12:40  
**Создатель:** GPT-5 Codex  
**Зависимости:** API-TASK-SOC-PO-Create, API-TASK-WORLD-PO-Validation

---

## 📋 Краткое описание

Создать OpenAPI спецификацию `economy-service` для расчёта бюджета и финансовых гарантий заказов игроков.

**Что нужно сделать:** Описать REST API и модели данных `api/v1/economy/player-orders/budget.yaml`, обеспечивающие расчёт формулы бюджета, escrow, комиссий и страховых пакетов, а также публикацию Kafka событий для social-service.

---

## 🎯 Цель задания

Обеспечить экономический модуль инструментами для детерминированного расчёта бюджета заказов и управления финансовыми гарантиями, синхронизированными с мастером создания заказов.

**Зачем это нужно:**
- Социалка использует единый сервис расчёта бюджета на шаге мастера.
- Экономика контролирует escrow, комиссии и страхование, снижая риск мошенничества.
- Вся финансовая логика фиксируется в отдельном контракте для codegen в backend/frontend.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`  
**Версия документа:** 1.0.0  
**Дата последнего обновления:** 2025-11-08 09:53  
**Статус документа:** approved  

**Что важно из этого документа:**
- Формула `BaseReward = ComplexityScore * RiskModifier * MarketIndex * TimeModifier`.
- Требования к escrow (10–30%), комиссиям (5–12%) и страховкам (базовая/расширенная/премиум).
- Необходимость предупреждений о завышении/занижении бюджета относительно медианы шаблона.
- UX ожидания: симулятор бюджета в мастере должен получать подробные расчёты и предупреждения.

### Дополнительные источники

- `.BRAIN/02-gameplay/social/player-orders-system-детально.md` — общий процесс, события, интеграции.
- `.BRAIN/02-gameplay/social/player-orders-economy.md` — экономические показатели и рыночные индексы (контекст).
- `.BRAIN/02-gameplay/social/player-orders-reputation-детально.md` — влияние бюджета на рейтинги исполнителей/заказчиков.
- JSON схемы: `schemas/social/player-order-budget.schema.json`, `schemas/social/player-order-publish.schema.json`.
- Готовая спецификация social-service: `api/v1/social/player-orders.yaml` (для согласования контрактов).

### Связанные документы

- [player-orders-creation-детально](../../../../.BRAIN/_02-gameplay/social/player-orders-creation-детально.md) — мастер создания.
- [player-orders-system-детально](../../../../.BRAIN/_02-gameplay/social/player-orders-system-детально.md) — сквозной процесс.
- [player-orders-economy](../../../../.BRAIN/_02-gameplay/social/player-orders-economy.md) — рыночные метрики.
- [player-orders-reputation-детально](../../../../.BRAIN/_02-gameplay/social/player-orders-reputation-детально.md) — рейтинговые эффекты.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/economy/player-orders/budget.yaml`  
**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── economy/
            └── player-orders/
                ├── budget.yaml      ← создать
                └── README.md        ← обновить/создать при необходимости (архитектурная схема)
```

Если поддиректории отсутствуют, создать их. Проверить ограничение 400 строк: при превышении разбить на логичные файлы (`budget.yaml`, `budget-components.yaml`).

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)
- **Микросервис:** economy-service  
- **Порт:** 8085  
- **API Base Path:** `/api/v1/economy/player-orders/*`  
- **Домен:** Финансовые операции заказов игроков, escrow и страхование  
- **Зависимости:** world-service (геополитический риск), social-service (брифы), telemetry-service (логирование), notification-service (уведомления о блокировках escrow)

### Frontend (модуль)
- **Модуль:** `modules/social/player-orders`  
- **Путь:** `modules/social/player-orders/budget`  
- **State Store:** `useSocialStore` (`playerOrders.budgetState`)  
- **UI компоненты (@shared/ui):** `BudgetSimulator`, `RiskIndicator`, `EscrowSummary`, `InsuranceTierCard`  
- **Формы (@shared/forms):** `BudgetAdjustForm`, `GuaranteeSelectionForm`  
- **Layouts (@shared/layouts):** `GameLayout`  
- **Хуки (@shared/hooks):** `useEconomyRates`, `useRealtime`, `useDebounce`

### Комментарий для OpenAPI
В начале `budget.yaml` добавить блок:
```
# Target Architecture:
# - Microservice: economy-service (port 8085)
# - Base Path: /api/v1/economy/player-orders/*
# - Frontend Module: modules/social/player-orders/budget
# - UI: BudgetSimulator, RiskIndicator, EscrowSummary, InsuranceTierCard
# - Forms: BudgetAdjustForm, GuaranteeSelectionForm
# - Hooks: useEconomyRates, useRealtime, useDebounce
# - Depends On: world-service (risk matrices), telemetry-service (budget audits)
```

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Анализ исходных документов
1. Выписать все параметры формулы бюджета и допустимые диапазоны.
2. Определить, как escrow и страховки зависят от результата расчёта.
3. Собрать бизнес-правила предупреждений (медиана по шаблону, лимиты).
4. Зафиксировать Kafka события, необходимые для синхронизации с социальным сервисом.

### Шаг 2: Определение API endpoints
Запланировать минимум следующие операции:
1. `POST /economy/player-orders/budget/estimate` — основной расчёт бюджета и escrow.
2. `POST /economy/player-orders/budget/compare` — сравнение пользовательского бюджета с референсными метриками и генерация предупреждений.
3. `GET /economy/player-orders/market-index` — получение актуальных рыночных индексов (можно фильтр по district/faction).
4. `GET /economy/player-orders/insurance-tiers` — справочник страховых пакетов и коэффициентов.
5. `POST /economy/player-orders/escrow/lock` — подтверждение блокировки escrow после публикации заказа (инициирует Kafka событие).

Для каждого endpoint описать: назначение, входные параметры, схемы запросов/ответов, коды ошибок (`400`, `401`, `403`, `409`, `422`, `500`) с ссылками на `shared/common/responses.yaml`.

### Шаг 3: Определение моделей данных
Создать схемы:
- `PlayerOrderBudgetEstimateRequest` — валидация входа (использовать структуру из `player-order-budget.schema.json` + расширения).
- `PlayerOrderBudgetEstimateResponse` — итоговые суммы, коэффициенты, рекомендации.
- `BudgetWarning` — структура предупреждений.
- `MarketIndexSnapshot` и `MarketIndexHistory` (для трендов).
- `InsuranceTier` — уровни страхования (название, покрытие, коэффициенты, обязательные условия).
- `EscrowLockRequest`/`EscrowLockResponse`.

Использовать `$ref` на общие компоненты (`pagination`, `responses`, `security`), избегать дублирования.

### Шаг 4: Создание OpenAPI спецификации
1. Собрать `openapi: 3.0.3`, `info`, `servers` (prod + staging).
2. Описать `securitySchemes` через `shared/common/security.yaml`.
3. В `tags` отразить разделы: `Player Order Budget`, `Market Metrics`, `Escrow`.
4. Заполнить `paths` с детальными описаниями и примерами (`example`).
5. В `components.schemas` разместить модели, в `components.parameters` — фильтры (`district`, `faction`, `template`).
6. Добавить `x-kafka-topics`:
   - `economy.player-orders.budget.calculated`
   - `economy.player-orders.escrow.locked`
   - `economy.player-orders.insurance.applied`

### Шаг 5: Валидация и проверка
1. Проверить спецификацию `npx swagger-cli validate api/v1/economy/player-orders/budget.yaml`.
2. Убедиться в соответствии JSON схемам (значения ограничений).
3. Пройти чеклист `tasks/config/checklist.md`.
4. Согласовать ссылки `$ref` с social-service спецификацией (указать в task).

---

## 🧩 Endpoints (детализация)

1. **POST `/api/v1/economy/player-orders/budget/estimate`**  
   - **Назначение:** Рассчитать базовый бюджет, escrow и комиссии по входным параметрам.  
   - **Параметры:** none (в теле).  
   - **Request Body:** `PlayerOrderBudgetEstimateRequest`.  
   - **Ответы:**  
     - `200 OK` — `PlayerOrderBudgetEstimateResponse` c полями `baseReward`, `escrow`, `commission`, `guaranteeSuggestions`, `medianDelta`.  
     - Типовые ошибки: `400`, `401`, `403`, `422`.  
   - **Примечания:** Включить массив `warnings[]` с кодами (`BUDGET_OVERESTIMATED`, `MARKET_SPIKE`, `RISK_HIGH`).  

2. **POST `/api/v1/economy/player-orders/budget/compare`**  
   - **Назначение:** Сравнить пользовательский бюджет и рекомендованный.  
   - **Request Body:** `PlayerOrderBudgetComparisonRequest` (поля `proposedBudget`, `templateId`, `districtCode`).  
   - **Ответы:**  
     - `200 OK` — `BudgetComparisonResult` (дельты, рекомендации, диапазоны).  
     - `409 Conflict` — ограничения escrow/страховки нарушены.  

3. **GET `/api/v1/economy/player-orders/market-index`**  
   - **Назначение:** Получить текущие и исторические `MarketIndex` значения.  
   - **Параметры:** `district`, `faction`, `interval` (enum: `15m`, `1h`, `24h`).  
   - **Ответ:** `200 OK` — `MarketIndexHistory`.  

4. **GET `/api/v1/economy/player-orders/insurance-tiers`**  
   - **Назначение:** Справочник пакетов страхования (`basic`, `extended`, `premium`).  
   - **Ответ:** `200 OK` — `InsuranceTierList`.  
   - **Коды ошибок:** `401`, `403`.  

5. **POST `/api/v1/economy/player-orders/escrow/lock`**  
   - **Назначение:** Заблокировать escrow после публикации заказа.  
   - **Request Body:** `EscrowLockRequest` (`orderId`, `ownerId`, `escrowAmount`, `insuranceTier`).  
   - **Ответы:**  
     - `202 Accepted` — запуск асинхронной операции + Kafka `economy.player-orders.escrow.locked`.  
     - `409 Conflict` — недостаточно средств.  
     - `404 Not Found` — счёт не найден.  

---

## 🧱 Модели данных

- `PlayerOrderBudgetEstimateRequest`: поля `complexityScore`, `riskModifier`, `marketIndex`, `timeModifier`, `templateId`, `districtCode`, `factionRelations`, `guaranteeTier`, `preferredCurrency`.  
- `PlayerOrderBudgetEstimateResponse`: `baseReward`, `escrow`, `commission`, `insuranceFee`, `recommendedBudgetRange`, `warnings[]`, `calculationBreakdown`.  
- `BudgetWarning`: `code`, `severity`, `message`, `suggestedAction`.  
- `MarketIndexSnapshot`: `timestamp`, `value`, `volatility`, `source`.  
- `MarketIndexHistory`: `interval`, `snapshots[]`.  
- `InsuranceTier`: `id`, `name`, `coverage`, `commissionRate`, `escrowMultiplier`, `bonusReputation`.  
- `EscrowLockRequest` / `EscrowLockResponse`: идентификаторы, суммы, статусы, тайм-ауты.  
- Общие перечисления: `RiskLevel`, `InsuranceTierCode`, `BudgetWarningCode`.  
- Обязательно добавить примеры (`example`) для ключевых схем.

---

## 📌 Принципы и правила

- Следовать правилам `API-SWAGGER/api-swagger-rules`.
- Соблюдать SOLID/DRY/KISS: выносить повторяющиеся схемы в `player-orders/budget-components.yaml` при необходимости.
- Использовать `shared/common/responses.yaml`, `pagination.yaml`, `security.yaml` через `$ref`.
- Версия API `1.0.0`, при изменениях соблюдать семантическое версионирование.
- Не хардкодить значения коэффициентов; только описывать их диапазоны и типы.
- Документировать Kafka события в `x-kafka-topics`.
- Добавить `x-codegen` подсказки для генерации SDK (имена клиентов/методов).

---

## ✅ Критерии приемки (минимум 10)

1. В `budget.yaml` присутствует блок `Target Architecture`.
2. Все пять endpoints описаны с полными запросами, ответами и кодами ошибок.
3. Основная формула и коэффициенты отражены в описаниях и примерах.
4. Схема `PlayerOrderBudgetEstimateRequest` валидирует диапазоны (`minimum`, `maximum`).
5. Схема ответа содержит `calculationBreakdown` с расшифровкой множителей.
6. Добавлены `examples` для успешного расчёта и сценария `budget overestimate`.
7. Включены Kafka события в `x-kafka-topics` c ссылками на payload схемы.
8. Все ссылки `$ref` указывают на существующие/shared компоненты (без дублирования стандартных ответов).
9. `swagger-cli validate` проходит без ошибок.
10. Файл ≤ 400 строк или разбит на несколько файлов.
11. Задокументированы требования к аутентификации (BearerAuth, роль `economy.operator`).
12. План проверки (unit + contract tests) указан в разделе FAQ.

---

## ❓ FAQ

- **Q:** Нужно ли добавлять ручное редактирование коэффициентов?  
  **A:** Нет, коэффициенты берутся из внутренних таблиц economy-service. API предоставляет только расчёт и предупреждения.

- **Q:** Как social-service получает медиану для предупреждений?  
  **A:** Через `budget/compare`, который возвращает `medianBudget` и `percentileRange` по шаблону и региону.

- **Q:** Что делать при всплесках `MarketIndex`?  
  **A:** Возвращать предупреждение `MARKET_SPIKE` с рекомендацией перепроверить заказ через 30 минут и указать `nextRefreshAt`.

- **Q:** Какие роли имеют доступ к `escrow/lock`?  
  **A:** Только сервисные аккаунты `social-service` и `arbitration-service` с scope `economy.player-orders.write`.

- **Q:** Нужна ли поддержка мультивалюты?  
  **A:** Да, предусмотреть поле `preferredCurrency` и список поддерживаемых валют в справочнике. Конвертация описывается в `calculationBreakdown`.

---

## 🧭 История выполнения

- 2025-11-08 12:40 — задача создана (GPT-5 Codex), статус `queued`.

---

**Следующие действия:** После генерации спецификации обновить `brain-mapping.yaml`, связать с документом `.BRAIN/_02-gameplay/social/player-orders-creation-детально.md`, а также уведомить social-service о доступности расчётного API.

