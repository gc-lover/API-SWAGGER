# Task ID: API-TASK-193
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-07 15:45
**Создатель:** GPT-5 Codex (API Task Creator)
**Зависимости:** none

---

## 📋 Краткое описание

Создать OpenAPI спецификацию голосовых лобби и party finder.

**Что нужно сделать:** Спроектировать API для управления голосовыми лобби, подбором групп, подканалами и модерацией на основе документа `voice-lobby-system.md`.

---

## 🎯 Цель задания

Обеспечить полный API-слой для Voice Lobby & Party Finder, чтобы социальные и рейдовые активности могли использовать централизованные голосовые лобби с автоматическим подбором, подканалами и контролем качества связи.

**Зачем это нужно:**
- Скоординированный voice onboarding перед рейдами, турнирами и социальными ивентами
- Интеграция голосовых каналов с party/guild/clan системами
- Единый API для UI, матчмейкера и микросервиса голосовой связи

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`
**Путь к документу:** `.BRAIN/05-technical/backend/voice-lobby/voice-lobby-system.md`
**Версия документа:** v1.0.0
**Дата последнего обновления:** 2025-11-07
**Статус документа:** approved

**Что важно из этого документа:**
- Типы голосовых лобби (activity, clan/guild, raid, tournament)
- SQL-схемы таблиц `voice_lobbies`, `lobby_participants`, `lobby_subchannels`
- Алгоритм Party Finder, критерии подбора и баланс по ролям
- Управление подканалами, роли модерации, ready-check
- События, уведомления и ключевые метрики аналитики

### Дополнительные источники

- `.BRAIN/05-technical/backend/voice-chat/voice-chat-system.md` – общая архитектура voice чата и провайдера
- `.BRAIN/05-technical/backend/party-system.md` – интеграция с party составом
- `.BRAIN/05-technical/backend/guild-system-backend.md` – наследование прав гильдии
- `.BRAIN/05-technical/backend/matchmaking/matchmaking-algorithm.md` – общие принципы подбора

### Связанные документы

- `.BRAIN/05-technical/backend/clan-war/clan-war-system.md` – клановые ивенты
- `.BRAIN/05-technical/backend/daily-reset/daily-reset-compact.md` – автоматическое управление окнами активности
- `.BRAIN/05-technical/backend/realtime-server/part1-architecture-zones.md` – взаимодействие с realtime уведомлениями

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/social/voice/voice-lobbies.yaml`
**API версия:** v1
**Тип файла:** OpenAPI 3.0 Specification (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── social/
            └── voice/
                └── voice-lobbies.yaml  ← создать/заполнить этот файл
```

**Если файл уже существует:**
- Обновить и расширить в рамках этого задания без дублирования общих компонентов
- Поддерживать версионирование (info.version ≥ 1.0.0)

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)

- **Целевой микросервис:** social-service
- **Порт:** 8084
- **API Base Path:** `/api/v1/social/voice-lobbies`
- **Домен:** Социальные коммуникации, голосовые лобби, матчмейкинг команд
- **Зависимости:**
  - auth-service (валидация токена и ролей)
  - party-service (синхронизация состава группы)
  - guild-service (права и ранги)
  - matchmaking-service (оценка рейтингов)
  - voice-provider-adapter (интеграция с WebRTC провайдером)

### Frontend (модульная архитектура)

- **Модуль:** `modules/social/voice-lobbies`
- **State Store:** `useSocialStore`
- **State срезы:** `voiceLobbies`, `matchmakingRequests`, `voiceSessions`
- **UI компоненты:** `VoiceLobbyCard`, `VoiceLobbyFilters`, `VoiceLobbyDetailsDrawer`, `VoiceStatusIndicator`
- **Формы:** `VoiceLobbyForm`, `ReadyCheckModal`, `SubchannelConfigForm`
- **Layouts:** `GameLayout`, `SocialHubLayout`
- **Хуки:** `useRealtime`, `useDebounce`, `usePartySync`

### Комментарий для OpenAPI файла

В начало `voice-lobbies.yaml` добавить блок:

```yaml
# Target Architecture:
# - Microservice: social-service (port 8084)
# - API Base: /api/v1/social/voice-lobbies
# - Dependencies: auth-service, party-service, guild-service, matchmaking-service, voice-provider-adapter
# - Frontend Module: modules/social/voice-lobbies (useSocialStore.voiceLobbies)
# - UI: VoiceLobbyCard, VoiceLobbyFilters, VoiceLobbyDetailsDrawer, VoiceStatusIndicator
# - Forms: VoiceLobbyForm, ReadyCheckModal, SubchannelConfigForm
# - Layouts: GameLayout, SocialHubLayout
# - Hooks: useRealtime, useDebounce, usePartySync
```

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Анализ и декомпозиция документа
- Сверить типы лобби, таблицы БД, алгоритмы подбора и события
- Выделить сущности (lobby, participant, subchannel, matchmaking request, ready-check)
- Зафиксировать бизнес-правила (ограничения ролей, статусы лобби, права модерации)

### Шаг 2: Определение структуры API
- Спроектировать `paths` под каталог `/api/v1/social/voice-lobbies`
- Определить методы для: поиска, создания, join/leave, ready-check, lock/unlock, подканалов, voice настроек, матчмейкинга, модерации
- Зафиксировать интеграционные ответы (events, party sync)

### Шаг 3: Модели данных и компоненты
- Создать схемы `VoiceLobby`, `LobbyParticipant`, `Subchannel`, `RoleRequirement`, `VoiceSettings`, `MatchmakingRequest`, `MatchmakingStatus`, `ReadyCheckState`, `ModerationAction`
- Вынести повторяющиеся структуры в `components/schemas`
- Использовать общие ответы из `api/v1/shared/common/responses.yaml`

### Шаг 4: Документация и примеры
- Добавить подробные descriptions и enum значения
- Подготовить примеры запросов/ответов для ключевых операций
- Описать события (webhooks/event bus) в разделе `components/schemas` или `x-events`

### Шаг 5: Валидация и соответствие стандартам
- Проверить связность с voice-chat и party API (описать зависимости в разделах Description)
- Убедиться, что все параметры имеют типы, ограничения и примеры
- Актуализировать версию API и лицензии/servers блоки

### Шаг 6: Контроль качества
- Прогнать check-list перед handoff
- Подготовить рекомендации по тестированию (postman/scenario) в описании задания

---

## 🔀 Endpoints

### 1. GET `/api/v1/social/voice-lobbies`
- **Назначение:** Поиск и фильтрация лобби
- **Параметры Query:** `activityCode`, `lobbyType`, `role`, `region`, `language`, `status`, `minRating`, `maxRating`, `page`, `size`
- **Ответ 200:** `VoiceLobbyListResponse` (список карточек лобби с требуемыми ролями, загрузкой и качеством связи)
- **Ошибки:** `400` (invalid filters), `401`, `429`

### 2. POST `/api/v1/social/voice-lobbies`
- **Назначение:** Создать новое лобби
- **Тело:** `CreateVoiceLobbyRequest` (тип лобби, activity, ограничения, роли, настройки качества)
- **Ответ 201:** `VoiceLobby` (полное описание созданного лобби)
- **Ошибки:** `400`, `401`, `403` (нет прав), `409` (активное лобби уже существует для игрока)

### 3. GET `/api/v1/social/voice-lobbies/{lobbyId}`
- **Назначение:** Получить детали лобби, участников и подканалов
- **Ответ 200:** `VoiceLobbyDetails`
- **Ошибки:** `401`, `403`, `404`

### 4. POST `/api/v1/social/voice-lobbies/{lobbyId}/join`
- **Назначение:** Присоединить игрока с выбранной ролью и голосовыми настройками
- **Тело:** `JoinLobbyRequest`
- **Ответ 200:** `LobbyParticipant` + актуальные subchannels
- **Ошибки:** `400`, `401`, `403`, `404`, `409` (роль занята), `422` (не проходит проверку рейтинга/уровня)

### 5. POST `/api/v1/social/voice-lobbies/{lobbyId}/leave`
- **Назначение:** Выйти из лобби и освободить слот
- **Ответ 200:** `LeaveLobbyResponse` (подтверждение и обновленная статистика)
- **Ошибки:** `401`, `403`, `404`

### 6. POST `/api/v1/social/voice-lobbies/{lobbyId}/ready-check`
- **Назначение:** Запустить ready-check и получить `readyCheckId`
- **Ответ 202:** `ReadyCheckState`
- **Ошибки:** `401`, `403`, `404`, `409` (уже запущено)

### 7. PATCH `/api/v1/social/voice-lobbies/{lobbyId}/lock`
- **Назначение:** Установить режим `locked/unlocked`, опциональное авто-закрытие
- **Тело:** `LockLobbyRequest`
- **Ответ 200:** `VoiceLobby` (обновленное состояние)
- **Ошибки:** `401`, `403`, `404`

### 8. POST `/api/v1/social/voice-lobbies/{lobbyId}/subchannels`
- **Назначение:** Создать/обновить/удалить подканалы (main, role, commander)
- **Тело:** `SubchannelMutationRequest`
- **Ответ 200:** `SubchannelList`
- **Ошибки:** `400`, `401`, `403`, `404`, `409`

### 9. PATCH `/api/v1/social/voice-lobbies/{lobbyId}/voice-settings`
- **Назначение:** Изменить качество аудио, ptt режим, интеграцию с провайдером
- **Тело:** `VoiceSettingsPatch`
- **Ответ 200:** `VoiceSettings`
- **Ошибки:** `400`, `401`, `403`, `404`

### 10. POST `/api/v1/social/voice-lobbies/matchmaking/search`
- **Назначение:** Отправить запрос на подбор подходящего лобби
- **Тело:** `MatchmakingRequest`
- **Ответ 202:** `MatchmakingStatus` (requestId, estimatedWait)
- **Ошибки:** `400`, `401`, `429`

### 11. GET `/api/v1/social/voice-lobbies/matchmaking/status/{requestId}`
- **Назначение:** Проверить статус matchmaker (подобрано/в очереди/создано новое лобби)
- **Ответ 200:** `MatchmakingStatus`
- **Ошибки:** `401`, `404`, `410`

### 12. POST `/api/v1/social/voice-lobbies/{lobbyId}/moderation/actions`
- **Назначение:** Выполнить модерационное действие (mute/unmute/kick/invite/promote)
- **Тело:** `ModerationActionRequest`
- **Ответ 200:** `ModerationResult`
- **Ошибки:** `401`, `403`, `404`, `409`

### 13. GET `/api/v1/social/voice-lobbies/analytics/overview`
- **Назначение:** Получить агрегированные метрики заполнения и качества
- **Параметры Query:** `activityCode`, `region`, `range`
- **Ответ 200:** `VoiceLobbyAnalytics`
- **Ошибки:** `401`, `403`

---

## 🧱 Модели данных

- **VoiceLobby** – идентификатор, тип, название, описание, activityCode, роли, лимиты, статус, timestamps, quality preset, voiceChannelId
- **VoiceLobbySummary** – сокращенная карточка для списков (участники, заполненность, требуемые роли)
- **RoleRequirement** – `role`, `needed`, `current`, `priority`
- **LobbyParticipant** – игрок, роль, micStatus (`PTT|VOX|MUTED`), readyStatus (`READY|PENDING|NOT_READY`), лидерские флаги
- **Subchannel** – id, purpose (`main|role|strategy|private`), maxParticipants, isLocked, passwordProtected
- **VoiceSettings** – режим (`high|standard|low`), `codec`, `bitrate`, `latencyTarget`, `pttMode`, `noiseSuppression`
- **CreateVoiceLobbyRequest** – параметры создания (тип, activityCode, owner info, регион, язык, requiredRoles, maxParticipants)
- **JoinLobbyRequest** – playerId, preferredRole, voiceSettingsOverrides, subchannelPreference
- **LockLobbyRequest** – `locked`, `reason`, `autoCloseAt`
- **SubchannelMutationRequest** – список операций (`create|update|delete`) с данными подканала
- **MatchmakingRequest** – profilные параметры (rating, level, roles[], activity, availability window, language)
- **MatchmakingStatus** – requestId, state (`queued|matched|created|expired`), suggestedLobbyId, estimatedWait, assignedRole
- **ReadyCheckState** – `readyCheckId`, `state (running|completed|failed)`, `expiresAt`, списки готовых/неготовых
- **ModerationActionRequest** – действие, цель, дополнительные параметры (duration, reason)
- **ModerationResult** – подтверждение действия и обновленная информация
- **VoiceLobbyAnalytics** – агрегаты (openLobbies, activeLobbies, avgWaitTime, avgFillTime, qualityMetrics)

Все модели должны иметь `description`, `required`, ограничения (`max`, `enum`, `pattern`), и примеры.

---

## 🧭 Принципы и правила

- Соблюдать SOLID/DRY/KISS – один YAML < 400 строк, переиспользование общих компонентов через `$ref`
- OpenAPI 3.0.3, `info.title` = `Voice Lobby API`, `servers` с prod/stage
- Использовать `components/responses` из `api/v1/shared/common/` через `$ref`
- Все идентификаторы в формате UUID, даты в ISO 8601
- События и интеграции описать через `x-integration` или отдельные схемы

---

## 🧪 Примеры

- Пример создания лобби (activity raid, роли tank/heal/dps, high quality)
- Пример join-запроса с VoiceSettings override (push-to-talk)
- Пример ответа ready-check с распределением статусов
- Пример статуса matchmaker (state `matched`, suggestedLobbyId)
- Пример модерационного действия mute с временем истечения

Каждый пример оформить в `application/json` с валидными данными.

---

## 🔗 Связности и зависимости

- Обязательно указать в описании endpoints взаимодействие с party-service (перенос состава), guild-service (наследование ролей) и voice-provider (создание каналов)
- События (`voice_lobby.created`, `voice_lobby.ready_check_started`, `voice_lobby.closed`) описать в разделе `components/schemas/EventEnvelope`
- Отметить, что API зависит от авторизации (`BearerAuth`) и rate limiting

---

## ✅ Критерии приемки

1. Создан файл `api/v1/social/voice/voice-lobbies.yaml` в формате OpenAPI 3.0.3
2. В файле присутствует архитектурный комментарий из секции Target Architecture
3. Все 13 endpoints задокументированы с методами, параметрами, requestBody и ответами
4. Все перечисленные модели данных определены в `components/schemas`
5. Использованы общие ответы и схемы безопасности из `api/v1/shared/common/`
6. Примеры запросов/ответов добавлены для ключевых операций (create, join, ready-check, matchmaking, moderation)
7. Описаны статусы и валидации (enum, min/max, patterns) для всех полей
8. Добавлены ссылки/описания интеграций с party, guild и voice provider
9. Документация содержит раздел о событиях и аналитике
10. OpenAPI файл проходит линтер (spectral/openapi) без ошибок
11. Сформирован план тестирования и FAQ внутри задания

---

## ❓ FAQ

- **Как сопоставить `/api/v1/voice-lobbies` из документа с архитектурой?** – Переносим в `/api/v1/social/voice-lobbies`, отражаем это в описаниях и gateway маршрутах.
- **Что делать при нехватке ролей или заполненном лобби?** – Возвращать `409 Conflict` с кодом `ROLE_SLOT_UNAVAILABLE`, описать схему ошибки.
- **Как управлять подканалами стороннего провайдера?** – API возвращает `voiceChannelId` и `subchannel.voiceChannelId`; фронтенд через Orval вызывает SDK провайдера.
- **Нужно ли хранить историю ready-check?** – Да, описать `ReadyCheckState` и события `ready_check_completed`.
- **Как отображать качество связи?** – В `VoiceSettings` предусмотреть поля `latency`, `packetLoss`, `qualityTier` и описать, что обновление идёт через analytics endpoint.
- **Как избежать дублирования party и lobby ID?** – Добавить рекомендацию использовать `partyId` в `VoiceLobby` и `MatchmakingStatus` для синхронизации.

---

## 🕓 История выполнения

- 2025-11-07 15:45 — Задание создано (GPT-5 Codex)

---

**Примечание:** Перед handoff пройти чеклист `tasks/config/checklist.md`.

