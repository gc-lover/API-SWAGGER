# Task ID: API-TASK-XXX
**Тип:** API Generation | API Update | API Validation
**Приоритет:** низкий | средний | высокий | критический
**Статус:** queued
**Создано:** YYYY-MM-DD HH:MM
**Создатель:** AI Agent (название)
**Зависимости:** [API-TASK-XXX] | none

---

## 📋 Краткое описание

Одна-две строки о задаче.

**Что нужно сделать:** Создать API спецификацию для X на основе документации Y.

---

## 🎯 Цель задания

Детальное описание цели и контекста.

**Зачем это нужно:**
- Причина задачи
- Бизнес-логика
- Связь с общими целями проекта

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`
**Путь к документу:** `.BRAIN/02-gameplay/combat/shooter-mechanics.md`
**Версия документа:** v1.0.0
**Дата последнего обновления:** 2025-11-02
**Статус документа:** approved | draft | review

**Что важно из этого документа:**
- Ключевые концепции, которые нужно отразить в API
- Бизнес-правила
- Ограничения и требования

### Дополнительные источники

- `.BRAIN/01-concepts/core-pillars.md` - основные столпы игры (для контекста)
- `.BRAIN/02-gameplay/combat/abilities/` - связанные механики способностей
- Существующие API: `API-SWAGGER/api/v1/gameplay/combat/abilities.yaml` (для примера формата)

### Связанные документы

- [Связанный документ 1](../../../../.BRAIN/path/to/doc.md) - описание связи
- [Связанный документ 2](../../../../.BRAIN/path/to/doc2.md) - описание связи

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/gameplay/combat/shooting.yaml`
**API версия:** v1
**Тип файла:** OpenAPI 3.0 Specification (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── gameplay/
            └── combat/
                └── shooting.yaml  ← Создать этот файл
```

**Если файл уже существует:**
- Обновить существующий файл
- Сохранить совместимость (версионирование при необходимости)
- Обновить документацию

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Анализ исходного документа

**Действия:**
1. Прочитать файл `.BRAIN/02-gameplay/combat/shooter-mechanics.md`
2. Выделить ключевые концепции:
   - Типы стрельбы (аркадная/реалистичная)
   - TTK (Time To Kill)
   - Система урона (слабые точки)
   - Движение и стрельба
3. Выделить бизнес-правила:
   - Ограничения на использование
   - Валидация параметров
   - Условия применения

**Ожидаемый результат:**
- Список концепций для отражения в API
- Список бизнес-правил

### Шаг 2: Определение API endpoints

**Что нужно создать:**

1. **POST `/api/v1/gameplay/combat/shoot`**
   - **Назначение:** Выполнить выстрел
   - **Параметры запроса:**
     - `weapon_id` (string, required) - ID оружия
     - `target_position` (object, required) - Позиция цели
     - `aim_point` (string, optional) - Точка прицеливания (head/body/limb)
   - **Ответы:**
     - `200 OK` - Успешный выстрел
     - `400 Bad Request` - Невалидные параметры
     - `404 Not Found` - Оружие не найдено
     - `409 Conflict` - Нет патронов

2. **GET `/api/v1/gameplay/combat/weapons`**
   - **Назначение:** Получить список доступного оружия
   - **Параметры запроса:**
     - `type` (string, optional) - Тип оружия
     - `rarity` (string, optional) - Редкость
   - **Ответ:** `200 OK` - Список оружия

3. **POST `/api/v1/gameplay/combat/weapons/{weapon_id}/reload`**
   - **Назначение:** Перезарядить оружие
   - **Path параметры:**
     - `weapon_id` (string, required) - ID оружия
   - **Ответы:**
     - `200 OK` - Успешная перезарядка
     - `404 Not Found` - Оружие не найдено

### Шаг 3: Определение моделей данных

**Что нужно создать:**

1. **Weapon** (оружие)
   ```yaml
   Weapon:
     type: object
     properties:
       id: string
       name: string
       type: string (enum: pistol, rifle, sniper, etc.)
       damage: integer
       fire_rate: integer
       accuracy: number
       ammunition:
         type: object
         properties:
           current: integer
           max: integer
           type: string
       rarity: string (enum: common, rare, epic, legendary)
   ```

2. **Shot** (выстрел)
   ```yaml
   Shot:
     type: object
     properties:
       weapon_id: string
       target_position:
         type: object
         properties:
           x: number
           y: number
           z: number
       aim_point: string (enum: head, body, limb)
       damage_dealt: integer
       hit: boolean
       timestamp: string (ISO 8601)
   ```

3. **Ammunition** (боеприпасы)
   ```yaml
   Ammunition:
     type: object
     properties:
       current: integer
       max: integer
       type: string
       reload_time: number
   ```

### Шаг 4: Создание OpenAPI спецификации

**Требования к файлу:**

1. **Структура OpenAPI 3.0:**
   ```yaml
   openapi: 3.0.3
   info:
     title: Combat Shooting API
     version: 1.0.0
     description: API для механик стрельбы
   servers:
     - url: https://api.necp.game/v1
       description: Production server
   paths:
     # Endpoints здесь
   components:
     schemas:
       # Модели данных здесь
   ```

2. **Обязательные элементы:**
   - ✅ OpenAPI версия: 3.0.3
   - ✅ Info блок с описанием
   - ✅ Servers блок
   - ✅ Paths со всеми endpoints
   - ✅ Components/schemas со всеми моделями
   - ✅ Примеры запросов/ответов
   - ✅ Коды ошибок
   - ✅ Валидация параметров

3. **Документация:**
   - Описание каждого endpoint
   - Описание каждой модели
   - Примеры использования
   - Описание ошибок

### Шаг 5: Валидация и проверка

**Что проверить:**

1. **Валидация YAML:**
   - Синтаксис YAML корректен
   - OpenAPI спецификация валидна

2. **Проверка полноты:**
   - Все endpoints из документации включены
   - Все модели данных описаны
   - Все бизнес-правила отражены

3. **Проверка соответствия:**
   - API соответствует документации из `.BRAIN`
   - Соответствует принципам проекта
   - Соответствует стандартам OpenAPI

4. **Проверка связности:**
   - Связанные API совместимы
   - Нет конфликтов с существующими API

---

## 🎨 Принципы и правила

### Общие принципы проекта

1. **API First** - сначала спецификация, потом реализация
2. **RESTful API** - стандартные HTTP методы и коды ответов
3. **Версионирование** - семантическое версионирование API
4. **Документация** - все endpoints, модели и примеры должны быть задокументированы

### Стиль кодирования

1. **Именование:**
   - Endpoints: kebab-case (`/api/v1/gameplay/combat/shoot`)
   - Модели: PascalCase (`Weapon`, `Shot`)
   - Параметры: snake_case (`weapon_id`, `target_position`)

2. **Формат:**
   - YAML формат
   - Отступы: 2 пробела
   - Строки не длиннее 100 символов

3. **Документация:**
   - Все поля должны иметь `description`
   - Все enum должны иметь описания значений
   - Примеры обязательны для основных endpoints

### Правила из workspace rules

1. **SOLID** - одна фича - один файл
2. **DRY** - избегать дублирования
3. **KISS** - простота решений
4. **Данные в БД** - не хардкодить, хранить в БД (для API это значит валидация и документация)

---

## 📝 Примеры и шаблоны

### Пример структуры endpoint:

```yaml
paths:
  /gameplay/combat/shoot:
    post:
      summary: Выполнить выстрел
      description: |
        Выполняет выстрел из указанного оружия в указанную позицию.
        Учитывает тип оружия, точку прицеливания и характеристики.
      operationId: shoot
      tags:
        - Combat
        - Shooting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShootRequest'
            examples:
              basic_shot:
                value:
                  weapon_id: "weapon_001"
                  target_position:
                    x: 10.5
                    y: 2.3
                    z: 5.1
                  aim_point: "head"
      responses:
        '200':
          description: Успешный выстрел
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Shot'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/WeaponNotFound'
        '409':
          $ref: '#/components/responses/NoAmmunition'
```

### Пример структуры модели:

```yaml
components:
  schemas:
    Weapon:
      type: object
      required:
        - id
        - name
        - type
        - damage
      properties:
        id:
          type: string
          description: Уникальный идентификатор оружия
          example: "weapon_001"
        name:
          type: string
          description: Название оружия
          example: "Cyber Pistol Mk.III"
        type:
          type: string
          enum: [pistol, rifle, sniper, shotgun, smg]
          description: Тип оружия
          example: "pistol"
        damage:
          type: integer
          description: Базовый урон оружия
          minimum: 1
          maximum: 1000
          example: 45
```

---

## 🔗 Связанные API и зависимости

### Зависимости (API, которые должны быть созданы/обновлены раньше):

- `api/v1/gameplay/combat/abilities.yaml` - должен существовать (для связи способностей с оружием)
- `api/v1/gameplay/combat/implants.yaml` - должен существовать (для связи имплантов с оружием)

### Связанные API (ссылаются друг на друга):

- `api/v1/gameplay/combat/abilities.yaml` - способности могут влиять на стрельбу
- `api/v1/gameplay/progression/skills.yaml` - навыки стрельбы влияют на характеристики

### Зависимости (задания, которые должны быть выполнены раньше):

- `API-TASK-XXX` - Создание API для способностей (если не выполнено, создать базовую структуру)

---

## ✅ Критерии приемки

Задание считается выполненным, если:

1. ✅ **Файл создан:** `api/v1/gameplay/combat/shooting.yaml` существует
2. ✅ **OpenAPI валидный:** Файл проходит валидацию OpenAPI 3.0
3. ✅ **Все endpoints:** Все требуемые endpoints описаны
4. ✅ **Все модели:** Все требуемые модели данных описаны
5. ✅ **Документация:** Все элементы задокументированы
6. ✅ **Примеры:** Примеры запросов/ответов включены
7. ✅ **Соответствие:** API соответствует документации из `.BRAIN`
8. ✅ **Валидация:** Все параметры имеют валидацию
9. ✅ **Ошибки:** Все возможные ошибки описаны
10. ✅ **Связность:** Связанные API совместимы

---

## ❓ Возможные вопросы и ответы

**Q: Что делать, если в исходном документе нет всех деталей?**
A: Использовать принципы и стиль проекта, создать логичную структуру на основе существующих API.

**Q: Как обрабатывать опциональные поля?**
A: Четко обозначить в схеме, какие поля required, какие optional. Добавить описание для каждого.

**Q: Что делать с бизнес-логикой, которая не относится к API?**
A: Описать в комментариях/description, но не реализовывать в API спецификации.

**Q: Нужно ли версионирование с первого дня?**
A: Да, использовать семантическое версионирование. Начать с v1.0.0.

---

## 📞 Дополнительная информация

**Если возникли вопросы:**
- Проверить связанные документы в `.BRAIN`
- Посмотреть примеры существующих API в `API-SWAGGER/api/v1/`
- Проверить правила в `.cursor/rules/`

**Полезные ссылки:**
- [OpenAPI Specification](https://swagger.io/specification/)
- [REST API Design](https://restfulapi.net/)
- [Workspace Rules](../../../../README.md)

---

## 📊 История выполнения

- `YYYY-MM-DD HH:MM` - Задание создано
- `YYYY-MM-DD HH:MM` - Назначено агенту
- `YYYY-MM-DD HH:MM` - Начато выполнение
- `YYYY-MM-DD HH:MM` - Завершено

---

**ВНИМАНИЕ:** Это задание для AI агента Cursor. Выполняй его пошагово, следуя всем инструкциям выше.
