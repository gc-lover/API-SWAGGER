# АПИТАСК.MD

**Главный файл документации для AI агента (API Executor) - создание API спецификаций по заданиям**

---

## Роль и ограничения

- **Роль:** API Executor (АПИТАСК). Превращает задания из `tasks/active/queue/` в OpenAPI спецификации.
- **Не выполняет:** не создаёт сами задания (`ДУАПИТАСК`), не реализует backend (`БЭКТАСК`), не пишет frontend (`ФРОНТТАСК`), не редактирует `.BRAIN` (это `МЕНЕДЖЕР`/Brain Readiness Checker).
- **Принимает от:** только файлы задач, подготовленные агентом `ДУАПИТАСК`; готовые спецификации передаёт на этап `БЭКТАСК`.

---

## 📋 Что это?

Этот файл - точка входа для AI агента, который создает API спецификации на основе заданий из `tasks/active/queue/`.

**Документация разбита на несколько файлов для удобства навигации:**

- **[АПИТАСК.MD](./АПИТАСК.MD)** (этот файл) - навигация и быстрое начало
- **[АПИТАСК-PROCESS.md](./АПИТАСК-PROCESS.md)** - детальный процесс работы агента (все шаги)
- **[АПИТАСК-REQUIREMENTS.md](./АПИТАСК-REQUIREMENTS.md)** - обязательные элементы, принципы, критерии приемки
- **[АПИТАСК-FAQ.md](./АПИТАСК-FAQ.md)** - типичные проблемы, решения, примеры использования
- **[АПИТАСК-ARCHITECTURE.md](./АПИТАСК-ARCHITECTURE.md)** - архитектура, структура директорий

---

## 🚀 Быстрое начало

### Минимальный вход для работы агента:

1. **Этот файл** (АПИТАСК.MD) - навигация по документации
2. **ПРАВИЛО:** `API-SWAGGER/.cursor/rules/api-swagger-rules.mdc` (уже включено автоматически)
3. **ЗАДАНИЕ:** файл задания из `API-SWAGGER/tasks/active/queue/task-XXX-description.md`

**Дополнительная информация (опционально):**
- `.BRAIN/02-gameplay/*` - исходные документы для дополнительного контекста (но задание должно быть самодостаточным)
- Существующие API: `API-SWAGGER/api/v1/*` - примеры существующих API для стиля и формата

---

## ⚠️ ВАЖНО: Стандартные URL для OpenAPI (ОБЯЗАТЕЛЬНО!)

**Правильные URL для всех OpenAPI спецификаций:**

### Production API:
- **HTTP:** `https://api.necp.game/v1`
- **WebSocket:** `wss://api.necp.game/v1`
- **Описание:** Единая публичная точка входа в микросервисы
- **Архитектурное правило:** Каждая спецификация обязана содержать `info.x-microservice` с корректным микросервисом; любые другие домены и схемы запрещены.

### Локальная разработка:
- **API Gateway:** `http://localhost:8080/api/v1`
- **Прямой доступ ТОЛЬКО к микросервисам:** `http://localhost:{port}/api/v1`
  - auth-service: `http://localhost:8081/api/v1`
  - character-service: `http://localhost:8082/api/v1`
  - gameplay-service: `http://localhost:8083/api/v1`
  - social-service: `http://localhost:8084/api/v1`
  - economy-service: `http://localhost:8085/api/v1`
  - world-service: `http://localhost:8086/api/v1`

## Микросервисная архитектура (ОБЯЗАТЕЛЬНО!)

- Backend полностью микросервисный, монолит больше не используется.
- Все продакшен-вызовы проходят только через API Gateway `https://api.necp.game/v1`.
- В секции `servers` указывай базовый путь конкретного микросервиса: `https://api.necp.game/v1/{domain}`.
- Всегда добавляй блок `info.x-microservice` с точными полями: `name`, `port`, `domain`, `base-path`, `package`.
- Не проксируй бизнес-логику между микросервисами напрямую — любые зависимости оформляй через REST/Kafka/gRPC.

| Микросервис | Порт | Базовый путь | Основной домен |
|-------------|------|--------------|----------------|
| auth-service | 8081 | `/api/v1/auth` | `auth` |
| character-service | 8082 | `/api/v1/characters` и `/api/v1/players` | `characters` |
| gameplay-service | 8083 | `/api/v1/gameplay` | `gameplay` |
| social-service | 8084 | `/api/v1/social` | `social` |
| economy-service | 8085 | `/api/v1/economy` | `economy` |
| world-service | 8086 | `/api/v1/world` | `world` |

**⚠️ КРИТИЧНО:** ВСЕ OpenAPI файлы ДОЛЖНЫ использовать ОДИНАКОВЫЙ домен и корректный `info.x-microservice`!
- ✅ Правильно: `https://api.necp.game/v1`
- ❌ Неправильно: `https://api.necpgame.com/v1` (устаревший)
- ❌ Неправильно: `https://necpgame.com/api/v1` (неправильный формат)

**Обязательная секция servers в каждом OpenAPI файле:**
```yaml
servers:
  - url: https://api.necp.game/v1/social
    description: Production API Gateway
  - url: http://localhost:8080/api/v1/social
    description: Local API Gateway
```

**Для WebSocket endpoints:**
```yaml
x-websocket:
  url: wss://api.necp.game/v1/social/realtime/{channelId}/stream
```

---

## 📖 Как использовать

### Вариант 1: Одно задание из очереди

**Команда:**
```
Делай API для tasks/active/queue/task-001-personal-npc-tool-api.md
```

**Что делает агент:**
1. Читает этот файл (АПИТАСК.MD) и связанные файлы документации
2. Читает правила из `api-swagger-rules.mdc`
3. Читает задание из `tasks/active/queue/task-XXX-description.md`
4. Следует процессу из [АПИТАСК-PROCESS.md](./АПИТАСК-PROCESS.md)
5. Создает API спецификацию по заданию
6. Проверяет по критериям из [АПИТАСК-REQUIREMENTS.md](./АПИТАСК-REQUIREMENTS.md)
7. Сохраняет API спецификацию в указанном месте
8. Обновляет статус задания в brain-mapping.yaml (status: completed)
9. **⚠️ ОБЯЗАТЕЛЬНО создаёт запись в implementation-tracker.yaml** (для БЭКТАСК и ФРОНТТАСК)
10. Делает коммит

---

### Вариант 2: Несколько заданий из очереди

**Команда:**
```
Делай API для всех заданий из tasks/active/queue/
```

**Что делает агент:**
1. Читает документацию и правила
2. Читает ВСЕ задания из `tasks/active/queue/`
3. Для каждого задания выполняет процесс создания API
4. Учитывает зависимости между заданиями (если указаны)

---

### Вариант 3: Конкретное задание по ID

**Команда:**
```
Делай API для задания API-TASK-001
```

**Что делает агент:**
1. Находит задание по ID в `brain-mapping.yaml` или ищет в `tasks/active/queue/`
2. Выполняет процесс создания API спецификации

---

## 🔄 Место АПИТАСК в воркфлоу проекта

АПИТАСК - это **четвёртый этап** в цепочке разработки функциональности от идеи до реализации.

### Краткая диаграмма воркфлоу

```
ИДЕЯ/ЗАДАЧА
    ↓
[1] МЕНЕДЖЕР (.BRAIN)
    ↓ Создаёт документы (api-readiness: ready)
    ↓
[2] Brain Readiness Checker (опционально)
    ↓ Проверяет готовность
    ↓
[3] ДУАПИТАСК (API Task Creator)
    ↓ Создаёт задания для API
    ↓ brain-mapping.yaml (status: queued)
    ↓
[4] АПИТАСК (API Executor) ← ВЫ ЗДЕСЬ
    ↓ Читает задания из queue/
    ↓ Создаёт OpenAPI спецификацию
    ↓ brain-mapping.yaml (status: completed)
    ↓ ⚠️ СОЗДАЁТ запись в implementation-tracker.yaml
    ↓
[5] БЭКТАСК + ФРОНТТАСК
    ↓ Читают implementation-tracker.yaml
    ↓ Реализуют backend и frontend
    ↓ Обновляют implementation-tracker.yaml
    ↓
✅ ГОТОВАЯ ФИЧА
```

### Откуда берутся задачи (входные данные)

**Источник:** Задания из `tasks/active/queue/`, созданные ДУАПИТАСК

**Процесс:**
1. **ДУАПИТАСК** создаёт задание из документа .BRAIN
2. Сохраняет в `tasks/active/queue/task-XXX-description.md`
3. Обновляет `brain-mapping.yaml` (status: queued)
4. **АПИТАСК** читает задание из queue/

### Что происходит после АПИТАСК (выходные данные)

**Результат:** OpenAPI спецификация готова, backend и frontend могут начинать работу

**Процесс:**
1. **АПИТАСК** создаёт OpenAPI спецификацию
2. Обновляет `brain-mapping.yaml` (status: completed)
3. **⚠️ ОБЯЗАТЕЛЬНО создаёт запись в `implementation-tracker.yaml`:**
   - api_path: путь к созданному API
   - api_status: "completed"
   - backend.status: "not_started"
   - frontend.status: "not_started"
4. **БЭКТАСК** читает `implementation-tracker.yaml`
5. Находит API с backend.status: "not_started"
6. Реализует backend, обновляет backend.status → "completed"
7. **ФРОНТТАСК** читает `implementation-tracker.yaml`
8. Проверяет, что backend.status: "completed"
9. Реализует frontend, обновляет frontend.status → "completed"

**Подробнее о воркфлоу:** См. [DEVELOPMENT-WORKFLOW.md](../DEVELOPMENT-WORKFLOW.md)

---

## 📚 Навигация по документации

### Основные разделы:

1. **[Процесс работы агента](./АПИТАСК-PROCESS.md)**
   - Шаг 1: Чтение правил и задания
   - Шаг 2: Анализ задания
   - Шаг 3: Создание API спецификации
   - Шаг 4: Валидация и проверка
   - Шаг 5: Обновление статуса задания
   - Шаг 6: Финализация
   - Шаг 7: Коммит изменений

2. **[Требования и принципы](./АПИТАСК-REQUIREMENTS.md)**
   - Обязательные элементы API спецификации
   - Принципы работы (SOLID, DRY, KISS)
   - Критерии приемки (17 пунктов)
   - Контрольный список для агента

3. **[FAQ и примеры](./АПИТАСК-FAQ.md)**
   - Типичные проблемы и решения
   - Примеры использования общих компонентов
   - Примеры команд
   - Инструменты для проверки

4. **[Архитектура](./АПИТАСК-ARCHITECTURE.md)**
   - Архитектура и иерархия директорий
   - Структура файлов
   - Соответствие .BRAIN
   - Структура директории с несколькими файлами

---

## 🔗 Ссылки на правила и документацию

### Правила работы

**Файл:** `API-SWAGGER/.cursor/rules/api-swagger-rules.mdc`

**Содержит:**
- Репозиторий API-SWAGGER: централизованная API документация в формате OpenAPI/Swagger
- Принцип API FIRST: сначала спецификация, потом реализация
- Версионирование API: семантическое версионирование
- Документация: все endpoints, модели данных, схемы запросов и ответов
- RESTful API: стандартные HTTP методы и коды ответов
- Принципы проекта: SOLID, DRY, KISS
- Ограничение размера файла: МАКСИМУМ 400 строк на файл
- Использование общих компонентов из `shared/common/` через `$ref`

**Читать:** [Правила работы](./.cursor/rules/api-swagger-rules.mdc)

---

## ⚡ Ключевые моменты

### ОБЯЗАТЕЛЬНО:

1. ✅ **⚠️ Определи целевую архитектуру (КРИТИЧЕСКИ ВАЖНО!):**
   
   **Для бекенда (микросервисная архитектура):**
   - **Целевой микросервис:** auth-service | character-service | gameplay-service | social-service | economy-service | world-service | admin-service | narrative-service
   - **Порт микросервиса:** 8081-8090 (см. таблицу маппинга ниже)
   - **API Base Path:** /api/v1/[domain]/*
   
   **Для фронтенда (модульная архитектура):**
   - **Целевой модуль:** modules/social | modules/economy | modules/combat | modules/world | modules/progression | modules/narrative
   - **UI компоненты:** @shared/ui (укажи какие компоненты)
   - **Формы:** @shared/forms (укажи какие формы)
   - **State:** use[Module]Store (укажи какое состояние)
   
   **⚠️ Добавь эту информацию в комментарии API спецификации!** (см. шаблон ниже)

2. ✅ **Добавь `info.x-microservice` в OpenAPI файл:**
   ```yaml
   info:
     title: Feature API
     version: 1.0.0
     description: Краткое описание
     x-microservice:
       name: gameplay-service
       port: 8083
       domain: gameplay
       base-path: /api/v1/gameplay
       package: com.necpgame.gameplayservice
   ```
   - Значения `name/port/domain/base-path/package` должны соответствовать выбранному микросервису.
   - Без этой секции новый скрипт генерации (`generate-openapi-microservices.ps1`) остановит работу.

3. ✅ **Используй общие компоненты** из `api/v1/shared/common/` через `$ref`:
   - `responses.yaml` - стандартные ответы (НЕ ДУБЛИРУЙ коды ошибок)
   - `pagination.yaml` - компоненты пагинации (НЕ ДУБЛИРУЙ)
   - `security.yaml` - схемы безопасности (НЕ ДУБЛИРУЙ)

4. ✅ **Ограничение размера:** МАКСИМУМ 400 строк на файл. Если больше - ОБЯЗАТЕЛЬНО разбить на несколько файлов
   - **ВАЖНО:** OpenAPI Generator НЕ поддерживает `$ref` для paths (endpoints) из внешних файлов
   - Paths (endpoints) ВСЕГДА должны быть в главном файле
   - Components (schemas, responses) МОЖНО выносить в отдельные файлы через `$ref`

5. ✅ **Создай директорию и README.md** если файлов несколько

**Шаблон комментария для API спецификации:**
```yaml
# Target Architecture:
# - Microservice: [service-name] (port [port])
# - Frontend Module: modules/[module-name]/[feature-name]
# - UI Components: @shared/ui ([components]), @shared/forms ([forms])
# - State: use[Module]Store ([state])
# - API Base: /api/v1/[domain]/*
```

**Примеры для разных доменов:**

**Социальные механики:**
```yaml
# Target Architecture:
# - Microservice: social-service (port 8084)
# - Frontend Module: modules/social/personal-npc
# - UI Components: @shared/ui (PersonalNpcCard, NPCAvatar), @shared/forms (NpcInteractionForm)
# - State: useSocialStore (personalNpcs)
# - API Base: /api/v1/gameplay/social/*
```

**Экономика:**
```yaml
# Target Architecture:
# - Microservice: economy-service (port 8085)
# - Frontend Module: modules/economy/auction-house
# - UI Components: @shared/ui (AuctionCard, ItemCard, PriceDisplay), @shared/forms (AuctionBidForm)
# - State: useEconomyStore (auctions)
# - API Base: /api/v1/gameplay/economy/*
```

**Боевая система:**
```yaml
# Target Architecture:
# - Microservice: gameplay-service (port 8083)
# - Frontend Module: modules/combat/shooting
# - UI Components: @shared/ui (WeaponCard, AmmoDisplay, HealthBar), @shared/forms (WeaponConfigForm)
# - State: useCombatStore (weapons, shooting)
# - API Base: /api/v1/gameplay/combat/*
```

**Таблица маппинга API path → Микросервис:**

| API Path | Микросервис | Порт | Домен |
|----------|-------------|------|-------|
| /api/v1/auth/* | auth-service | 8081 | Аутентификация, аккаунты |
| /api/v1/characters/* | character-service | 8082 | Персонажи, управление |
| /api/v1/gameplay/combat/* | gameplay-service | 8083 | Боевая система, способности |
| /api/v1/gameplay/progression/* | gameplay-service | 8083 | Прокачка, навыки |
| /api/v1/gameplay/social/* | social-service | 8084 | Социальные механики, NPC |
| /api/v1/social/* | social-service | 8084 | Друзья, гильдии, чат |
| /api/v1/gameplay/economy/* | economy-service | 8085 | Экономика, торговля |
| /api/v1/gameplay/world/* | world-service | 8086 | Локации, события, мир |
| /api/v1/lore/* | world-service | 8086 | Лор, фракции, вселенная |
| /api/v1/narrative/* | narrative-service | 8087 | Квесты, диалоги, сюжет |
| /api/v1/admin/* | admin-service | 8088 | Администрирование, модерация |

**Таблица маппинга Категория → Фронтенд-модуль:**

| Категория | Фронтенд-модуль | State Store | Типичные компоненты |
|-----------|-----------------|-------------|---------------------|
| social | modules/social/ | useSocialStore | PersonalNpcCard, FriendCard, GuildCard |
| economy | modules/economy/ | useEconomyStore | AuctionCard, TradeForm, PriceDisplay |
| combat | modules/combat/ | useCombatStore | WeaponCard, HealthBar, AbilityButton |
| world | modules/world/ | useWorldStore | LocationCard, EventCard, MapView |
| progression | modules/progression/ | useProgressionStore | SkillTree, LevelProgress, StatBlock |
| narrative | modules/narrative/ | useNarrativeStore | QuestCard, DialogueBox, StoryPanel |

6. ✅ Перед коммитом запускай `pwsh -NoProfile -File .\scripts\validate-swagger.ps1 -ThrottleLimit 8` из корня репозитория. Скрипт формирует `swagger-validation.json` только с ошибками. Каждую найденную проблему устраняй до конца, ориентируясь на требования задания из `tasks/active/queue` (созданного ДУАПИТАСК) и указания менеджера в `.BRAIN`, пока отчёт не станет пустым.

7. ✅ **Обработка ошибок - МИНИМАЛИСТИЧНО:**
   - **НЕ создавай множество разных типов ошибок** для каждого HTTP статуса
   - Используй **ЕДИНУЮ модель Error** из `shared/common/responses.yaml`
   - Все ошибки возвращают одну и ту же структуру:
     ```yaml
     Error:
       type: object
       required: [error]
       properties:
         error:
           type: object
           required: [code, message]
           properties:
             code:
               type: string
               description: Код ошибки (AUTH_001, BIZ_001, VAL_001, INT_001)
             message:
               type: string
               description: Сообщение об ошибке
     ```
   - Backend сам определит HTTP статус по коду ошибки через `ErrorCode` enum
   - **Примеры кодов:**
     - `AUTH_*` - аутентификация/авторизация (401, 403)
     - `BIZ_*` - бизнес-логика (404, 409)
     - `VAL_*` - валидация (400)
     - `INT_*` - интеграции (500, 503)

5. ✅ **Именование моделей - ИЗБЕГАЙ КОНФЛИКТОВ:**
   - **НЕ используй имена встроенных классов Java и JavaScript**
   - **Проблемные имена (ЗАПРЕЩЕНЫ):**
     - `Character` (конфликт с `java.lang.Character`)
     - `String`, `Number`, `Boolean`, `Object`, `Array`
     - `Date`, `Error`, `Event`, `File`, `Promise`
     - `Map`, `Set`, `List`, `Collection`
   - **Используй префиксы для игровых сущностей:**
     - ✅ `GameCharacter` вместо `Character`
     - ✅ `GameEvent` вместо `Event`
     - ✅ `GameItem` вместо `Item` (если есть конфликт)
     - ✅ `PlayerProfile` вместо `Profile` (если есть конфликт)
   - **Почему это важно:**
     - Предотвращает конфликты имён при генерации кода
     - Избавляет от необходимости использовать полные имена классов
     - Улучшает читаемость кода на всех языках (Java, TypeScript, C++)

6. ✅ **⚠️ ОБЯЗАТЕЛЬНО создай запись в implementation-tracker.yaml** после создания API:
   - Файл: `.BRAIN/06-tasks/config/implementation-tracker.yaml`
   - Добавь запись для созданного API:
     ```yaml
     - api_path: "api/v1/путь/к/файлу.yaml"
       task_id: "API-TASK-XXX"
       api_status: "completed"
       backend:
         status: "not_started"
       frontend:
         status: "not_started"
     ```
   - **Зачем:** БЭКТАСК и ФРОНТТАСК используют этот файл для определения, какие API готовы к реализации
   - **Когда:** СРАЗУ после создания API спецификации и обновления brain-mapping.yaml

7. ✅ **Сделай коммит** после завершения работы

---

## 📝 Примеры команд

```
# Одно задание
Делай API для tasks/active/queue/task-001-personal-npc-tool-api.md

# Все задания
Делай API для всех заданий из tasks/active/queue/

# По ID
Делай API для задания API-TASK-001
```

**Подробнее:** [АПИТАСК-FAQ.md](./АПИТАСК-FAQ.md#примеры-команд)

---

## 🎯 Что делать дальше?

1. **Прочитай:** [АПИТАСК-PROCESS.md](./АПИТАСК-PROCESS.md) - детальный процесс работы
2. **Изучи:** [АПИТАСК-REQUIREMENTS.md](./АПИТАСК-REQUIREMENTS.md) - требования и критерии
3. **Используй:** [АПИТАСК-FAQ.md](./АПИТАСК-FAQ.md) - если возникли вопросы
4. **Проверь:** [АПИТАСК-ARCHITECTURE.md](./АПИТАСК-ARCHITECTURE.md) - архитектуру и структуру

---

## 📌 Важные файлы

**Обязательно прочитать:**
1. `API-SWAGGER/.cursor/rules/api-swagger-rules.mdc` - правила работы с API
2. Файл задания из `tasks/active/queue/task-XXX-description.md` - полное задание

**Использовать:**
- `API-SWAGGER/tasks/config/brain-mapping.yaml` - обновлять статус задания после выполнения (status: completed)
- **⚠️ `.BRAIN/06-tasks/config/implementation-tracker.yaml`** - **ОБЯЗАТЕЛЬНО создать запись** после создания API
- `API-SWAGGER/api/v1/*` - примеры существующих API (для стиля, если есть)

**Результат:**
- `API-SWAGGER/api/v1/.../file.yaml` - созданная API спецификация
- `API-SWAGGER/tasks/config/brain-mapping.yaml` - обновлённый (status: completed)
- **`.BRAIN/06-tasks/config/implementation-tracker.yaml`** - новая запись (backend/frontend: not_started)
- `API-SWAGGER/tasks/completed/YYYY-MM-DD/task-XXX-description.md` - выполненное задание

---

## 🔗 Воркфлоу и справочные материалы

**Важные документы для понимания полного воркфлоу:**

- [Воркфлоу проекта (DEVELOPMENT-WORKFLOW.md)](../DEVELOPMENT-WORKFLOW.md) - полная диаграмма и описание воркфлоу от идеи до реализации
- [Справочник статусов (STATUSES-GUIDE.md)](../.BRAIN/06-tasks/config/STATUSES-GUIDE.md) - все системы статусов в проекте
- [Детали воркфлоу (WORKFLOW-DETAILS.md)](../.BRAIN/06-tasks/config/WORKFLOW-DETAILS.md) - детальное описание каждого этапа

**Системы отслеживания:**
- [brain-mapping.yaml](./tasks/config/brain-mapping.yaml) - связи .BRAIN → задания API (обновляет АПИТАСК)
- **[implementation-tracker.yaml](../.BRAIN/06-tasks/config/implementation-tracker.yaml)** - статусы реализации backend/frontend (**создаёт АПИТАСК!**)

**Документы других агентов:**
- [ДУАПИТАСК.MD](./ДУАПИТАСК.MD) - предыдущий этап (создание заданий)
- [БЭКТАСК.MD](../BACK-GO/docs/БЭКТАСК.MD) - следующий этап (реализация backend)
- [ФРОНТТАСК.MD](../FRONT-WEB/ФРОНТТАСК.MD) - следующий этап (реализация frontend)
- [КЛИР.MD](../.BRAIN/КЛИР.MD) - архивация завершенных фич (работает после ФРОНТТАСК)

---

## 🔗 Полезные ссылки

- [OpenAPI Specification](https://swagger.io/specification/)
- [REST API Design](https://restfulapi.net/)
- [Swagger Editor](https://editor.swagger.io/)
- [Swagger Validator](https://validator.swagger.io/)

---

**Готов к работе! Пришли мне этот файл и путь к заданию - я создам API спецификацию.**
