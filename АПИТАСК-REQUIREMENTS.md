# АПИТАСК-REQUIREMENTS.md

**Обязательные элементы, принципы работы и критерии приемки**

📖 **Навигация:** [АПИТАСК.MD](./АПИТАСК.MD) | [АПИТАСК-PROCESS.md](./АПИТАСК-PROCESS.md) | [АПИТАСК-FAQ.md](./АПИТАСК-FAQ.md) | [АПИТАСК-ARCHITECTURE.md](./АПИТАСК-ARCHITECTURE.md)

---

## Обязательные элементы API спецификации

Каждая созданная API спецификация должна содержать:

### 1. Версионирование

- `info.version` - версия API (семантическое версионирование: 1.0.0, 1.1.0, 2.0.0)
- Версия должна соответствовать версии документа .BRAIN (если указана в задании)
- При обновлении API - увеличивать версию согласно семантическому версионированию
- Формат версии: `MAJOR.MINOR.PATCH` (например: 1.0.0, 1.1.0, 2.0.0)

### 2. Размер файлов (ОБЯЗАТЕЛЬНО)

- **МАКСИМУМ 400 строк на файл** - это жесткое ограничение
- Если файл превышает 400 строк - **ОБЯЗАТЕЛЬНО** разбить на несколько файлов
- **КРИТИЧЕСКИ ВАЖНО:** OpenAPI Generator и стандарт OpenAPI 3.0 **НЕ ПОДДЕРЖИВАЮТ** `$ref` для paths (endpoints) из внешних файлов
- **Правильное разбиение:**
  - **Paths (endpoints)** - ВСЕГДА должны быть в главном файле. НЕЛЬЗЯ выносить paths в отдельные файлы через `$ref`
  - **Components** (schemas, responses, parameters) - МОЖНО выносить в отдельные файлы через `$ref`
- Примеры именования при разбиении:
  - `resource-name.yaml` (главный файл с info, servers, ВСЕМИ paths, и ссылками на components через `$ref`)
  - `resource-name-models.yaml` (только components/schemas - модели данных, до 400 строк)
  - `resource-name-requests.yaml` (только components/schemas - request модели, до 400 строк)
  - **НЕ СОЗДАВАТЬ** `resource-name-endpoints.yaml` - все endpoints должны быть в главном файле
- Если paths слишком много и файл превышает 400 строк:
  - Сгруппировать endpoints логически (по функциональности)
  - Оставить все paths в главном файле, но структурировать их по группам с комментариями
  - Вынести модели в отдельные файлы через `$ref` для уменьшения размера
- Создать README.md с описанием структуры разбиения (если файлов несколько)
- **ВАЖНО:** При разбиении используй общие компоненты из `shared/common/` через `$ref`

### 3. Инлайн комментарии для исполнителя

- В начале файла (в `info.description`): комментарии о назначении API, источниках, версиях
- В секциях `paths`: комментарии к сложным endpoints с пояснениями бизнес-логики
- В секциях `components.schemas`: комментарии к моделям данных с пояснениями
- Формат комментариев:
  ```yaml
  # Комментарий для исполнителя: пояснение кода ниже
  # Источник: .BRAIN/02-gameplay/social/personal-npc-tool.md (v1.0.0)
  ```
- Комментарии должны быть полезными и пояснять:
  - Назначение кода
  - Бизнес-логику (если неочевидно)
  - Ограничения и валидации
  - Связи с другими API или системами

### 4. Ссылки на оригинальные файлы

- В `info.description`: полная ссылка на документ .BRAIN с версией
- В комментариях к endpoints: ссылки на соответствующие секции документа .BRAIN
- В комментариях к моделям: ссылки на определения моделей в документе .BRAIN
- Формат ссылок:
  ```yaml
  # Источник: .BRAIN/02-gameplay/social/personal-npc-tool.md (v1.0.0)
  # Раздел: Модели данных -> PersonalNPC
  # Задание: API-SWAGGER/tasks/active/queue/task-001-personal-npc-tool-api.md
  ```
- Все ссылки должны быть полными и указывать на конкретные секции документа

### 5. OpenAPI 3.0.3 структура

- `openapi: 3.0.3` - версия OpenAPI
- `info` - информация об API (title, version, description со ссылками)
- `servers` - серверы (Production server URL)
- `paths` - все endpoints
- `components` - схемы, responses, security schemes
- **ОБЯЗАТЕЛЬНО:** Используй общие компоненты из `shared/common/`:
  - `components/responses` - ссылки на `shared/common/responses.yaml`
  - `components/parameters` - ссылки на `shared/common/pagination.yaml`
  - `components/securitySchemes` - ссылки на `shared/common/security.yaml`
- **НЕ ДУБЛИРУЙ** стандартные компоненты - всегда используй `$ref`

### 6. Все endpoints из задания

- HTTP метод (GET, POST, PUT, DELETE, PATCH)
- Путь (`/api/v1/gameplay/social/npc`)
- Summary и description
- Parameters (path, query, header)
- Request body (если есть)
- Responses (200, 400, 401, 403, 404, 409, 422, 500)
- Examples для запросов/ответов
- Tags для группировки

### 7. Все модели данных из задания

- Type (object, string, integer, number, boolean, array)
- Properties (все поля)
- Required (обязательные поля)
- Validation (min, max, enum, format, pattern)
- Description для каждого поля
- Example для каждой модели

### 8. Документация

- Description для каждого endpoint
- Description для каждой модели и поля
- Description для всех enum значений
- Примеры использования
- Описание ошибок

### 9. Валидация

- Все параметры имеют валидацию
- Все поля моделей имеют валидацию
- Все enum имеют описание значений

### 10. Обработка ошибок

- **ОБЯЗАТЕЛЬНО:** Используй стандартные ответы из `shared/common/responses.yaml`
- Все возможные ошибки описаны через `$ref` на стандартные ответы
- Коды ошибок: 400 (Bad Request), 401 (Unauthorized), 403 (Forbidden), 404 (Not Found), 409 (Conflict), 422 (Unprocessable Entity), 500 (Internal Server Error)
- **НЕ СОЗДАВАЙ** свои версии стандартных ответов - всегда используй `$ref`:
  ```yaml
  responses:
    '400':
      $ref: '../../shared/common/responses.yaml#/components/responses/BadRequest'
  ```
- Примеры ответов с ошибками уже включены в стандартные ответы

---

## Принципы работы

1. **Следование заданию:** API спецификация должна точно соответствовать заданию - все endpoints и модели из задания должны быть реализованы

2. **Самодостаточность:** Задание содержит всю информацию для работы, не нужно обращаться к .BRAIN документам (но можно для дополнительного контекста)

3. **Валидность:** API спецификация должна быть валидной OpenAPI 3.0.3 - проверить через Swagger Editor

4. **Полнота:** Все элементы из задания должны быть реализованы - endpoints, модели, примеры, документация

5. **Соответствие принципам:** API должно соответствовать принципам проекта (SOLID, DRY, KISS, API First, RESTful)

6. **Версионирование:** ОБЯЗАТЕЛЬНО вести версионирование API через `info.version` (семантическое версионирование: 1.0.0, 1.1.0, 2.0.0)

7. **Ограничение размера:** Каждый файл API не должен превышать 400 строк. Если больше - ОБЯЗАТЕЛЬНО разбить на несколько файлов с логической структурой. Используй общие компоненты из `shared/common/` через `$ref` для уменьшения размера файлов

8. **Переиспользование компонентов (DRY):** ОБЯЗАТЕЛЬНО используй общие компоненты из `shared/common/`:
   - Стандартные ответы (responses.yaml) - НЕ ДУБЛИРУЙ коды ошибок
   - Пагинация (pagination.yaml) - НЕ ДУБЛИРУЙ компоненты пагинации
   - Безопасность (security.yaml) - НЕ ДУБЛИРУЙ схемы безопасности
   - Всегда используй `$ref` для ссылок на общие компоненты

9. **Инлайн комментарии:** Добавлять полезные комментарии для исполнителя (бекенд разработчика), поясняющие назначение, бизнес-логику, ограничения

10. **Ссылки на оригиналы:** Везде, где возможно, добавлять ссылки на оригинальные документы .BRAIN с указанием версий и конкретных секций

---

## Контрольный список для агента

### Перед началом работы:

- [ ] Прочитал `api-swagger-rules.mdc`
- [ ] Прочитал задание полностью
- [ ] Понял все endpoints из задания
- [ ] Понял все модели данных из задания
- [ ] Проверил зависимости (если есть)
- [ ] Понял критерии приемки

### После создания API спецификации:

- [ ] Файл(ы) создан(ы) по указанному пути в задании
- [ ] Целевой путь соответствует архитектуре директорий из `ARCHITECTURE.md`
- [ ] Каждый файл не превышает 400 строк (если больше - разбито на несколько файлов)
- [ ] Если файлов несколько - создана директория и README.md с описанием структуры
- [ ] Используются общие компоненты из `shared/common/` через `$ref` (НЕ ДУБЛИРУЮТСЯ)
- [ ] Версионирование указано в `info.version` (семантическое версионирование)
- [ ] В `info.description` есть ссылки на оригинальные документы .BRAIN
- [ ] Инлайн комментарии для исполнителя добавлены (к сложным местам)
- [ ] Ссылки на оригинальные файлы .BRAIN добавлены везде, где возможно
- [ ] OpenAPI 3.0.3 структура корректна
- [ ] Все endpoints из задания реализованы
- [ ] Все модели данных из задания реализованы
- [ ] Все элементы задокументированы
- [ ] Все примеры включены
- [ ] Все валидации указаны
- [ ] Стандартные ответы используют `$ref` на `shared/common/responses.yaml`
- [ ] API спецификация валидна (проверено через Swagger Editor)
- [ ] Соответствует критериям приемки из задания
- [ ] Проверено соответствие структуре `.BRAIN` → `API-SWAGGER`
- [ ] Обновлен статус в `brain-mapping.yaml`
- [ ] Задание перемещено в `tasks/completed/`
- [ ] **Сделан коммит** изменений (для сохранения истории)

---

## Критерии приемки (общие)

API спецификация считается выполненной, если:

1. ✅ **Файл(ы) создан(ы):** API спецификация существует по указанному пути
2. ✅ **Размер файлов:** Каждый файл не превышает 400 строк (если больше - разбито на несколько файлов)
3. ✅ **Структура директории:** Если файлов несколько - создана директория и README.md с описанием структуры
4. ✅ **Использование общих компонентов:** Используются общие компоненты из `shared/common/` через `$ref` (НЕ ДУБЛИРУЮТСЯ коды ошибок, пагинация, безопасность)
5. ✅ **Версионирование:** Версия API указана в `info.version` (семантическое версионирование)
6. ✅ **Ссылки на оригиналы:** В `info.description` есть ссылки на оригинальные документы .BRAIN с версиями
7. ✅ **Инлайн комментарии:** Добавлены полезные комментарии для исполнителя (к сложным местам, бизнес-логике)
8. ✅ **Ссылки в комментариях:** В комментариях к endpoints и моделям есть ссылки на соответствующие секции документа .BRAIN
9. ✅ **OpenAPI валидный:** Файл(ы) проходят валидацию OpenAPI 3.0.3
10. ✅ **Все endpoints:** Все endpoints из задания описаны
11. ✅ **Все модели:** Все модели данных из задания описаны
12. ✅ **Документация:** Все элементы задокументированы
13. ✅ **Примеры:** Примеры запросов/ответов включены
14. ✅ **Валидация:** Все параметры имеют валидацию
15. ✅ **Ошибки:** Все возможные ошибки описаны через `$ref` на стандартные ответы из `shared/common/responses.yaml`
16. ✅ **Соответствие заданию:** API соответствует заданию (все требования выполнены)
17. ✅ **Соответствие принципам:** API соответствует принципам проекта (SOLID, DRY, KISS)

---

📖 **Навигация:** [АПИТАСК.MD](./АПИТАСК.MD) | [АПИТАСК-PROCESS.md](./АПИТАСК-PROCESS.md) | [АПИТАСК-FAQ.md](./АПИТАСК-FAQ.md) | [АПИТАСК-ARCHITECTURE.md](./АПИТАСК-ARCHITECTURE.md)

